# 试题管理系统 · 技术文档

> 版本：v1.9.0（DS 直出模式分支 `ds-text-only`）
> 更新日期：2026-03-21（含 v1.9.0：Layer 0 节点列表替代 leaf_level、文档类型自动检测、PPT 过渡页源头过滤）

---

## 目录

1. [项目概述](#1-项目概述)
2. [整体架构](#2-整体架构)
3. [目录结构](#3-目录结构)
4. [数据库设计](#4-数据库设计)
5. [后端 API 文档](#5-后端-api-文档)
   - 5.1 [核心题库与组卷 API](#51-核心题库与组卷-api)
   - 5.2 [AI 知识点提取 API](#52-ai-知识点提取-api)
     - [单个知识点读写](#单个知识点读写)
     - [知识点 Excel 导出](#知识点-excel-导出)
     - [AI 批量标注知识类型与认知维度](#ai-批量标注知识类型与认知维度)
   - 5.3 [知识图谱可视化 API](#53-知识图谱可视化-api)
6. [前端设计](#6-前端设计)
7. [AI 知识点提取流程详解](#7-ai-知识点提取流程详解)
8. [配置与环境变量](#8-配置与环境变量)
9. [启动方式](#9-启动方式)
10. [打包部署（macOS DMG）](#10-打包部署macos-dmg)
11. [测试体系](#11-测试体系)
12. [已知问题与修复记录](#12-已知问题与修复记录)
13. [依赖清单](#13-依赖清单)

---

## 1. 项目概述

**试题管理系统**是一套面向高校教师的本地化教学工具，核心功能包括：

| 功能模块 | 说明 |
|----------|------|
| 题库管理 | 增删改查、批量操作、富文本 + 图片支持 |
| 智能组卷 | 按题型/数量/科目自动抽题，支持手动调整 |
| Word 导出 | 生成标准试卷 Word 文档，支持中英双语 |
| AI 知识点提取 | 上传教材 Markdown，DeepSeek 逐章提取结构化知识点 |
| 知识图谱出题 | 基于知识图谱调用 DeepSeek 直出题目 |
| 知识图谱可视化 | D3.js 力导向图展示文档-章节-知识点三层关系 |
| 知识点 Excel 导出 | 将知识图谱导出为《批量导入知识点模板》标准格式，含层级结构、教学属性、关系 ID |

**定位**：`ds-text-only` 分支为简化版，去除向量数据库、OCR、RAG 检索等重型依赖，仅保留 DeepSeek API 直出模式，适合轻量化部署。

---

## 2. 整体架构

```
┌─────────────────────────────────────────────────────┐
│                    浏览器（单页应用）                  │
│  index.html  ·  kg.html  ·  纯 Vanilla JS / CSS      │
└────────────────────┬────────────────────────────────┘
                     │ HTTP / JSON
┌────────────────────▼────────────────────────────────┐
│              Flask Web Server（port 5000）            │
│                                                     │
│  Blueprint: main (routes.py)                        │
│    ├── 题库 CRUD        /api/questions/*             │
│    ├── 组卷管理         /api/exams/*                 │
│    ├── 题型管理         /api/question-types/*        │
│    ├── 图片存储         /api/images/*                │
│    ├── 课程设置         /api/course-settings         │
│    └── 导入/导出        /api/questions/import|export │
│                                                     │
│  Blueprint: rag (rag_routes.py)                     │
│    ├── 文档上传解析     /api/rag/ds-upload           │
│    ├── 知识点异步提取   /api/rag/ds-extract/*        │
│    ├── 任务状态轮询     /api/rag/ds-tasks/*          │
│    ├── 知识点读写       /api/rag/ds-kps/<id>         │
│    ├── 知识点 Excel 导出 /api/rag/ds-export-xlsx     │
│    ├── AI批量属性标注   /api/rag/ds-batch-classify   │
│    ├── 知识图谱出题     /api/rag/ds-generate         │
│    ├── API 配置读写     /api/rag/config              │
│    └── 图谱数据         /api/rag/ds-graph            │
│                                                     │
│  Blueprint: kg (kg_routes.py)                       │
│    ├── 可视化页面       /kg                          │
│    ├── 图谱节点/边      /api/kg/graph                │
│    └── Chunk 原文       /api/kg/chunks               │
└───────────┬──────────────────────┬──────────────────┘
            │                      │
┌───────────▼────────┐  ┌──────────▼──────────────────┐
│  exam_system.db    │  │  ds_knowledge.db             │
│  (Flask-SQLAlchemy)│  │  (原生 sqlite3)              │
│                    │  │                              │
│  questions         │  │  ds_docs                    │
│  exams             │  │  ds_chapters                │
│  exam_questions    │  │  ds_kps                     │
│  question_types    │  │                              │
│  question_images   │  └──────────────────────────────┘
│  course_settings   │
└────────────────────┘
            │
┌───────────▼────────────────────────────────────────┐
│               DeepSeek API（外部调用）               │
│   model: deepseek-chat                              │
│   用途：① 知识点结构化提取  ② 直出出题              │
└────────────────────────────────────────────────────┘
```

---

## 3. 目录结构

```
试卷生成-simple/
├── server.py                # 开发模式入口（Flask dev server）
├── launcher.py              # 打包版入口（PyInstaller，自动打开浏览器）
├── config.py                # Flask 配置类（Dev / Prod / Test）
├── requirements.txt         # 生产依赖（5 个包）
├── VERSION                  # 版本号文件（当前 1.6.2）
├── .env                     # API Key 存储（不入 git）
│
├── app/
│   ├── factory.py           # create_app()，注册 Blueprint，DB 初始化
│   ├── db_models.py         # SQLAlchemy ORM 模型定义
│   ├── routes.py            # 核心 Blueprint：题库/组卷/导入导出
│   ├── rag_routes.py        # AI Blueprint：DS 直出知识提取 + 出题
│   ├── kg_routes.py         # KG Blueprint：知识图谱可视化数据
│   ├── utils.py             # Word 生成、图片存储、题目解析工具
│   ├── docx_importer.py     # 富文本 Word 导入（含图片/表格）
│   └── models.py            # Pydantic-like 业务模型（旧版，兼容保留）
│
├── templates/
│   ├── index.html           # 主界面（单页应用，7 个标签页）
│   └── kg.html              # 知识图谱可视化页面（D3.js）
│
├── ds_knowledge.db          # DS 模式知识图谱数据库
├── exam_system.db           # 主题库数据库
│
├── rag_uploads/             # 上传文件临时存储目录
├── uploads/
│   └── images/              # 题目图片磁盘存储
├── exports/                 # 导出文件临时目录
├── temp/                    # 上传解析临时目录
│
├── rag_pipeline/            # 独立 RAG 管线（老版，参考用）
│   ├── __main__.py
│   ├── config.py
│   ├── db.py
│   ├── parser.py
│   ├── kg_extractor.py
│   ├── question_generator.py
│   └── prompts.py
│
└── tests/                   # pytest 测试套件（115 个测试）
    ├── conftest.py
    ├── test_question_api.py
    ├── test_exam_api.py
    ├── test_models.py
    └── ...
```

---

## 4. 数据库设计

### 4.1 `exam_system.db`（Flask-SQLAlchemy）

#### `questions` — 题目表

| 字段 | 类型 | 说明 |
|------|------|------|
| `question_id` | TEXT PK | 格式：`q_YYYYMMDD_HHMMSS_N` |
| `question_type` | TEXT | 题型名称（如 `单选`、`简答`） |
| `content` | TEXT | 题目正文（支持 HTML 富文本） |
| `options` | TEXT | JSON 数组，选项列表 |
| `answer` | TEXT | 标准答案 |
| `reference_answer` | TEXT | 参考答案（简答/论述） |
| `explanation` | TEXT | 解析说明 |
| `content_en` | TEXT | 英文题目（双语模式） |
| `options_en` | TEXT | JSON 数组，英文选项 |
| `subject` | TEXT | 科目（用于按科目组卷/筛选） |
| `knowledge_point` | TEXT | 知识点标签 |
| `tags` | TEXT | 逗号分隔标签 |
| `difficulty` | TEXT | 难度：`easy`/`medium`/`hard` |
| `language` | TEXT | `zh`/`en`/`both` |
| `metadata_json` | TEXT | JSON 扩展字段 |
| `is_used` | BOOLEAN | 是否已组入试卷 |
| `used_date` | DATETIME | 使用日期 |
| `created_at` | DATETIME | 创建时间 |
| `updated_at` | DATETIME | 最近修改时间 |

#### `exams` — 试卷表

| 字段 | 类型 | 说明 |
|------|------|------|
| `exam_id` | TEXT PK | 格式：`exam_xxxxxxxx` |
| `name` | TEXT | 试卷名称 |
| `config` | TEXT | JSON 配置（题型 → 数量/分值） |
| `subject` | TEXT | 科目过滤条件 |
| `is_confirmed` | BOOLEAN | 是否最终确认（确认后禁止改题） |
| `confirmed_at` | DATETIME | 确认时间 |
| `created_at` / `updated_at` | DATETIME | 时间戳 |

#### `exam_questions` — 关联表（多对多）

| 字段 | 说明 |
|------|------|
| `exam_id` | FK → exams |
| `question_id` | FK → questions |
| `position` | 题目在试卷中的顺序编号 |

#### `question_types` — 题型表

| 字段 | 说明 |
|------|------|
| `id` | 自增主键 |
| `name` | 内部名称，如 `单选`、`简答>计算` |
| `label` | 显示名称，如 `计算题` |
| `has_options` | 是否有选项（选择题 = True） |
| `is_builtin` | 内置题型不可删除 |

**内置题型**（7 种）：单选、多选、是非、简答、计算题、论述题、材料分析题

#### `question_images` — 图片元数据表

| 字段 | 说明 |
|------|------|
| `image_id` | 格式：`img_xxxxxxxx` |
| `question_id` | 所属题目（可为 null，图片先于题目上传时） |
| `field` | `content`/`reference_answer`/`explanation` |
| `filename` | 磁盘文件名 |
| `content_type` | MIME 类型 |
| `file_size` | 字节数 |

#### `course_settings` — 课程设置表（单行配置）

| 字段 | 说明 |
|------|------|
| `course_name` | 课程名称 |
| `course_code` | 课程代码 |
| `exam_format` | 开卷/闭卷 |
| `exam_method` | 笔试/口试 |
| `target_audience` | 适用对象 |
| `institution_name` | 学校/机构名称 |
| `semester_info` | 学期信息 |
| `exam_title` | 试卷标题 |
| `paper_label` | 试卷标签（A/B） |

---

### 4.2 `ds_knowledge.db`（原生 sqlite3）

AI 知识点提取专用数据库，与 Flask ORM 解耦，使用 Python 原生 `sqlite3` 操作。

#### `ds_docs` — 文档记录

| 字段 | 说明 |
|------|------|
| `doc_id` | 文件名 stem，截取前 40 字符 |
| `filename` | 原始文件名 |
| `subject` | 学科（用户填写） |
| `status` | `uploaded` / `extracting` / `paused` / `done` / `error` |
| `error_msg` | 出错信息 |
| `created_at` | ISO 时间戳 |

#### `ds_chapters` — 节级别原文

v1.6.3 起，表存储的是**节**（最低标题级别内容块），而非整章。每个节记录自己的父章信息，以便上层聚合展示。v1.8.0 起新增 `section_name` 支持三级文档。

| 字段 | 类型 | 说明 |
|------|------|------|
| `doc_id` | TEXT | 所属文档 |
| `chapter_num` | INTEGER | 全局节序号（1-based，提取单元唯一标识） |
| `chapter_name` | TEXT | L2 节标题（若只有两级文档则为叶子节标题） |
| `parent_chapter_num` | INTEGER | L1 父章序号（1-based，默认 0 表示无父章） |
| `parent_chapter_name` | TEXT | L1 父章标题（用于知识点分组展示） |
| `section_name` | TEXT | L3 小节名（叶子，三级文档才有值；两级文档为 `''`） |
| `raw_text` | TEXT | 节原文（提取时截断至 8000 字） |

**层级语义对照**：
```
parent_chapter_name = L1 章名（最高层分组）
chapter_name        = L2 节名（中间层，两级文档时即为叶子）
section_name        = L3 小节名（叶子，三级文档专用）
```

> **迁移兼容**：旧库缺少的列在启动时自动 `ALTER TABLE` 追加（`parent_chapter_num` 默认 `0`，`parent_chapter_name`/`section_name` 默认 `''`）。

#### `ds_kps` — 知识点

| 字段 | 类型 | 说明 |
|------|------|------|
| `doc_id` | TEXT | 所属文档 |
| `chapter_name` | TEXT | **L1 父章**名称（用于 UI 分组展示） |
| `chapter_num` | INTEGER | L1 父章序号 |
| `section_name` | TEXT | **L2 节名**（三级文档的中间层；两级文档为 `''`；用于图谱筛选显示"章名 > 节名"） |
| `kp_name` | TEXT | 知识点名称（5-20 字） |
| `kp_content` | TEXT | 知识点详细说明（200-500 字） |
| `relations_json` | TEXT | JSON 数组，知识点间关系 |
| `teaching_focus` | TEXT | 教学要点，分号分隔，取值范围：`重点`、`难点`、`考点`（可多选，如 `重点;难点`） |
| `knowledge_type` | TEXT | 知识类型（单选）：`事实性` / `概念性` / `程序性` / `元认知` |
| `cognitive_dimension` | TEXT | 认知维度（单选）：`记忆` / `理解` / `应用` / `分析` / `评价` / `创造` |

> **迁移兼容**：`teaching_focus`、`knowledge_type`、`cognitive_dimension` 为 v1.7.1 新增；`section_name` 为 v1.8.0 新增。旧库启动时自动 `ALTER TABLE` 追加（默认值均为 `''`）。

**`relations_json` 数组元素格式**：
```json
{"type": "从属", "target": "目标知识点名称"}
```

**关系类型**：从属、比较、并列、交叉、前提

#### `ds_doc_refs` — 参考文献记录

> 新增于 v1.7.2。独立存储各章节的"主要参考文献"内容，不参与知识点导出，也不作为 DeepSeek 提取时的上下文。

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | INTEGER PK | 自增主键 |
| `doc_id` | TEXT | 所属文档 |
| `chapter_num` | INTEGER | 来源章节序号（对应 `ds_chapters.chapter_num`） |
| `ref_text` | TEXT | 参考文献原文 |

> **自动迁移**：`_ensure_tables()` 在建表时同步将 `ds_chapters` 中 `chapter_name LIKE '%参考文献%'` 的行写入本表（避免重复写入）。

---

## 5. 后端 API 文档

### 5.1 核心题库与组卷 API

**Blueprint 名**：`main`，无前缀

---

#### 图片存储

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/images/upload` | 上传图片，返回 `{image_id, url}` |
| GET | `/api/images/<image_id>` | 获取图片文件 |
| DELETE | `/api/images/<image_id>` | 删除图片 |

---

#### 题库管理

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/questions` | 查询题目，支持多条件过滤 |
| POST | `/api/questions` | 新增题目 |
| GET | `/api/questions/<id>` | 获取单题 |
| PUT | `/api/questions/<id>` | 更新题目 |
| DELETE | `/api/questions/<id>` | 删除单题（含图片） |
| POST | `/api/questions/batch-delete` | 批量删除 |
| POST | `/api/questions/batch-update-type` | 批量修改题型 |
| GET | `/api/questions/subjects` | 获取所有科目列表 |
| POST | `/api/questions/import` | 导入题目（.docx / .txt） |
| GET | `/api/questions/export` | 导出题目（json / csv） |

**GET `/api/questions` 查询参数**：

| 参数 | 说明 |
|------|------|
| `keyword` | 题目内容模糊搜索 |
| `type` | 题型精确匹配 |
| `language` | `zh`/`en`/`both` |
| `difficulty` | `easy`/`medium`/`hard` |
| `knowledge_point` | 知识点模糊搜索 |
| `is_used` | `1`=已用，`0`=未用 |
| `subject` | 科目精确匹配 |

**导入去重逻辑**：以题目 `content` 字段去空格后的文本作为唯一键，已存在则跳过并计入 `skipped`。

---

#### 组卷管理

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/exams` | 获取所有试卷 |
| POST | `/api/exams` | 新建试卷（手动） |
| GET | `/api/exams/<id>` | 获取试卷详情 |
| PUT | `/api/exams/<id>` | 更新试卷名称/配置 |
| DELETE | `/api/exams/<id>` | 删除试卷 |
| POST | `/api/exams/generate` | 按配置自动组卷 |
| POST | `/api/exams/<id>/add_question` | 向试卷添加题目 |
| DELETE | `/api/exams/<id>/remove_question/<qid>` | 从试卷移除题目 |
| POST | `/api/exams/<id>/replace_question` | 替换试卷中的题目 |
| POST | `/api/exams/<id>/reorder` | 调整题目顺序 |
| POST | `/api/exams/<id>/confirm` | 最终确认试卷 |
| GET | `/api/exams/<id>/export` | 导出为 Word 文档 |

**`/api/exams/generate` 请求体示例**：
```json
{
  "name": "期末考试A卷",
  "subject": "农业经济学",
  "config": {
    "单选": {"count": 20, "points": 2},
    "简答": {"count": 4, "points": 10}
  }
}
```

自动组卷策略：从 `is_used=False` 的题目中按题型依次抽取，优先满足数量要求，不足时取全部可用题目。

**`/api/exams/<id>/export` 查询参数**：

| 参数 | 默认 | 说明 |
|------|------|------|
| `mode` | `zh` | `zh`/`en`/`both` |
| `show_answer` | `1` | `0` 隐藏答案（生成学生版试卷） |

---

#### 题型管理

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/question-types` | 获取所有题型（按 id 排序） |
| POST | `/api/question-types` | 创建自定义题型 |
| PUT | `/api/question-types/<id>` | 更新题型名称/标签/是否有选项 |
| DELETE | `/api/question-types/<id>` | 删除自定义题型（有题目引用时拒绝） |

---

#### 课程设置

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/course-settings` | 读取课程配置（返回最新一行） |
| PUT | `/api/course-settings` | 写入课程配置（upsert） |

---

#### 其他

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/templates/download` | 下载题库导入模板（.docx） |

---

### 5.2 AI 知识点提取 API

**Blueprint 名**：`rag`，无前缀

---

#### API Key 配置

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/rag/config` | 读取 DeepSeek API Key（返回时不脱敏） |
| PUT | `/api/rag/config` | 写入 API Key 到 `.env` |

PUT 请求体：
```json
{"deepseek_api_key": "sk-xxxxxxxx"}
```

`.env` 写入逻辑：原地更新已有 `DEEPSEEK_API_KEY` 行，不存在则追加；同时清空旧版 `DEEPSEEK_TOKEN` 字段。

---

#### 文档管理

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/rag/ds-docs` | 列出所有上传文档及统计信息 |
| DELETE | `/api/rag/ds-docs/<doc_id>` | 删除文档及其所有章节/知识点 |
| GET | `/api/rag/ds-docs/<doc_id>/kps` | 获取指定文档的章节列表和知识点 |

**GET `/api/rag/ds-docs` 返回示例**：
```json
[
  {
    "doc_id": "农业经济学-导论",
    "filename": "农业经济学-导论.md",
    "subject": "农业经济学",
    "status": "done",
    "error_msg": "",
    "kp_count": 42,
    "chapter_count": 8,
    "ch_with_kps": 8
  }
]
```

---

#### 文档上传与章节解析

**POST `/api/rag/ds-upload`**

- **支持格式**：`.md`、`.txt`、`.docx`
- **表单字段**：`file`（文件）、`subject`（学科名，可选）
- 上传文件保存到 `rag_uploads/`，`doc_id` 为文件名 stem 前 40 字符

**章节/节解析流程**（v1.9.0 重构，Layer 0 返回 extraction_nodes 节点列表 + 文档类型自动检测）：

```
读取文件文本
    │
    ▼
_clean_ocr_md(text)
    │ 去除 OCR 文件头、页码标记行，合并多余空行
    │ 返回 (cleaned_text, had_markers)
    │
    ▼
_extract_all_headings(cleaned_text)
    │ 提取所有 h1-h6 标题（含层级 + 位置）
    │ 返回 [{level, text, pos}, ...]（按位置排序）
    │
    ▼
_detect_doc_type(headings)   ← v1.9.0 新增
    │ 根据 H1 占比 + 层级深度判断：'ppt' 或 'textbook'
    │ PPT 型：H1>=3次 且 H1占比>=20% 且 最深层级<=3
    │
    ▼
本地 _analyze_doc_structure() 生成 fallback_hierarchy
    │ chapter_level, section_level，extraction_nodes=[]
    │
    ├─ 若 DeepSeek API Key 已配置 → _clean_headings_with_ai()
    │      doc_type='ppt'  → PPT 专用 prompt（max_tokens=1200）：
    │        · 只从 H1 标题中选取；H2 子项不进入任何列表
    │        · non_content_texts：封面/过渡套语/纯问句(?)H1/无关内容/重复H1
    │        · extraction_nodes：有实质知识的 H1 标题列表
    │        · 强制覆盖：chapter_level=1, section_level=2
    │      doc_type='textbook' → 教材专用 prompt（max_tokens=800）：
    │        · 粒度目标：每节点 1-3 个知识点（100-500 字）
    │        · H3 有 H4 子节时选 H4，无子节时选 H3 本身
    │        · 三阶段后处理：展开→去重→完整性补全（按文档顺序）
    │      返回 {chapter_level, section_level, non_content_texts, extraction_nodes}
    │      │
    │      └─ extraction_nodes 为空 / JSON 失败 / API 错误 → 降级 fallback_hierarchy
    │
    ▼
有 extraction_nodes → _parse_md_by_nodes(cleaned_text, hierarchy)   ← v1.9.0 新增
    │ Step 1：过滤 non_content_texts（章级别只删标题行；节级别删标题+内容）
    │ Step 2：提取过滤后文本的所有标题及位置
    │ Step 3：按 extraction_nodes 集合标记节点标题（is_node=True）
    │ Step 4：对每个节点标题，追踪章/节上下文（跳过 non_content，同级兄弟不互为父）
    │ Step 5：内容边界 = min(下一个节点标题, 下一个 level<=node_h.level 的非节点标题)
    │         注：用 node_h.level 而非 section_level，PPT H1 节点包含其下所有 H2 子项
    │ Step 6：body 非空时写入结果条目
    │
无 extraction_nodes（降级）→ _parse_md_multilevel(cleaned_text, hierarchy)
    │ 按三级切分（chapter_level/section_level/leaf_level），兼容旧逻辑
    │
    ▼
存入 ds_docs（status='uploaded'）
    + ds_chapters（含 parent_chapter_* + section_name 字段）
```

**Layer 0 失败降级策略**：

| 情况 | 处理 |
|------|------|
| API Key 未设置 | 直接用 `_analyze_doc_structure` 本地分析，降级到 `_parse_md_multilevel` |
| JSON 解析失败 / API 错误 | 同上降级 |
| `extraction_nodes` 为空 | 同上降级 |
| PPT 文档 AI 返回 chapter_level≠1 | 强制覆盖为 `chapter_level=1, section_level=2` |

**成功响应**：
```json
{
  "success": true,
  "doc_id": "农业经济学-导论",
  "chapter_count": 3,
  "section_count": 8,
  "chapters": [
    {"num": 1, "name": "第一章 农业的内涵与特征"},
    {"num": 2, "name": "第二章 农业的地位与功能"},
    {"num": 3, "name": "第三章 农业经营制度"}
  ]
}
```

> `chapter_count` 为去重后的父章数，`section_count` 为实际提取单元数（节数）。

---

#### 知识点异步提取

**POST `/api/rag/ds-extract/<doc_id>`**

请求体：
```json
{
  "subject": "农业经济学",
  "resume": false
}
```

- `resume=true`：跳过已提取章节（断点续传），仅处理 `ds_kps` 中尚无记录的章节
- 启动后台线程（`threading.Thread`，`daemon=True`），立即返回 `task_id`

**DeepSeek 调用参数**：`model=deepseek-chat`，`temperature=0.3`，`max_tokens=3000`

**提示词核心要求**：
- 提取 3-8 个核心知识点
- `content` 字段引用原文关键表述（200-500 字），含定义/特征/分类/示例
- 仅标注同章节内知识点之间的关系
- 直接输出纯 JSON（不加 ` ``` ` 代码块标记）

**章节文本截断**：超过 8000 字时截断并附加提示语，防止超出 API context 限制。

**返回**：
```json
{"success": true, "task_id": "a1b2c3d4", "total": 8}
```

---

#### 任务状态轮询

**GET `/api/rag/ds-tasks/<task_id>`**

```json
{
  "status": "running",
  "message": "正在提取第 3/8 章：农业的地位与功能",
  "doc_id": "农业经济学-导论",
  "progress": 2,
  "total": 8,
  "total_chapters": 8,
  "kp_count": 14
}
```

`status` 取值：`running` / `done` / `error` / `paused`

---

#### 暂停任务

**POST `/api/rag/ds-tasks/<task_id>/pause`**

设置 `pause_requested=True` 标志，后台线程在当前章节完成后检查该标志并退出循环（非强制终止）。暂停后可通过 `resume=true` 继续。

---

#### 单个知识点读写

**GET `/api/rag/ds-kps/<kp_id>`**

返回单个知识点完整信息：

```json
{
  "id": 42,
  "doc_id": "农业经济学-导论",
  "chapter_name": "第一章 农业的内涵与特征",
  "chapter_num": 1,
  "name": "狭义农业与广义农业",
  "content": "...",
  "relations": [
    {"type": "从属", "target": "农业内涵体系"},
    {"type": "比较", "target": "广义农业概念"}
  ],
  "teaching_focus": "重点;难点",
  "knowledge_type": "概念性",
  "cognitive_dimension": "理解"
}
```

`kp_id` 不存在时返回 `404 {"error": "知识点 X 不存在"}`。

---

**PUT `/api/rag/ds-kps/<kp_id>`**

更新知识点，请求体：

```json
{
  "name": "更新后的名称",
  "content": "更新后的内容...",
  "relations": [
    {"type": "并列", "target": "另一知识点"}
  ],
  "teaching_focus": "重点;考点",
  "knowledge_type": "概念性",
  "cognitive_dimension": "理解"
}
```

- `name` 为空时返回 `400`
- `relations` 非列表时返回 `400`
- `teaching_focus`、`knowledge_type`、`cognitive_dimension` 均为可选，不传则清空（写入 `''`）
- 自动过滤 `target` 为空的无效关系条目
- 成功返回 `{"success": true, "id": 42}`；`kp_id` 不存在返回 `404`

---

#### 知识点 Excel 导出

**GET `/api/rag/ds-export-xlsx?doc_id=<doc_id>`**

将指定文档的知识图谱导出为《批量导入知识点模板》标准格式的 `.xlsx` 文件，可直接上传至课程知识点管理平台。

**查询参数**：

| 参数 | 必填 | 说明 |
|------|------|------|
| `doc_id` | ✅ | 文档 ID（同 `/api/rag/ds-docs` 返回的 `doc_id`） |

**Excel 文件格式**（与《批量导入知识点模板》完全对齐）：

| 列 | 字段名 | 来源 |
|----|--------|------|
| A | ID | 按导出行顺序自动分配，从 1 开始连续编号 |
| B | 一级知识点 | `ds_docs.doc_id`（去前缀数字、替换下划线为空格） |
| C | 二级知识点 | `ds_chapters.parent_chapter_name`（父章去重） |
| D | 三级知识点 | `ds_chapters.chapter_name`（节标题，跳过与父章同名的情况） |
| E | 四级知识点 | `ds_kps.kp_name` |
| H | 节点类型 | 章节行填 `知识单元`；知识点行填 `知识点` |
| I | 教学要点 | `ds_kps.teaching_focus`（分号分隔） |
| J | 知识类型 | `ds_kps.knowledge_type` |
| K | 认知维度 | `ds_kps.cognitive_dimension` |
| L | 前序知识点ID | `relations_json` 中 `type="前提"` 的 target 解析为导出 ID |
| M | 关联知识点ID | `relations_json` 中 `type` 为 `并列`/`交叉`/`比较` 的 target 解析为导出 ID |

**模板说明**：
以项目根目录的 `批量导入知识点模板.xlsx` 为基础，**保留第 1–2 行**（表头与说明行）不变，从**第 3 行**起写入知识点数据。若模板文件不存在则退化为程序自建表头。

**层级构建规则**（v1.8.0 重写，从 `ds_kps` 重建层级，不再依赖 `ds_chapters`）：

层级完全从 `ds_kps` 的 `(chapter_name, section_name)` 字段按出现顺序去重构建：

- L1（一级）= 文档节点，永远一行
- L2（二级）= `chapter_name`（L1 父章），按首次出现顺序去重
- L3（三级，有节名时）= `section_name`（L2 节），归属该章，按首次出现顺序去重
- L4（四级，有节名时）= 知识点，紧随其所属节之后
- L3（三级，无节名时）= 知识点直接挂在章下

**两种文档类型的排列示例**：

两级文档（`section_name = ''`）：
```
L1: 文档
  L2: 第一章 绪论
    L3: 知识点1（直接）
    L3: 知识点2
  L2: 第二章 理论基础
    L3: 知识点3
```

三级文档（`section_name` 有值）：
```
L1: 文档
  L2: 第一章 绪论
    L3: 第一节 研究背景
      L4: 知识点1
      L4: 知识点2
    L3: 第二节 研究目的
      L4: 知识点3
```

**非内容节过滤**：以下章节类型不参与导出（但其 `raw_text` 保留在 `ds_chapters` 中供 DeepSeek 提取时参考，主要参考文献内容另存至 `ds_doc_refs`）：

```python
_NON_CONTENT_KEYWORDS = (
    '学习目标', '小结', '关键词', '复习思考题', '思考题', '参考文献', '练习题'
)
```

过滤逻辑（`_is_non_content(name)`）：去除首尾空白及 `【】` 括号后，只要包含以上任意关键词即跳过（L2 父章级和 L3 子节级分别独立过滤）。

**关系 ID 解析**：`relations_json` 中 target 以名称存储；导出时按名称在整个文件中查找对应 export_id，`target` 格式支持 `章节名>知识点名`（取 `>` 后的部分匹配）。

**响应**：HTTP 200，`Content-Type: application/vnd.openxmlformats-officedocument.spreadsheetml.sheet`，附件下载，文件名为 `<doc_label>_知识点导出.xlsx`。

**错误响应**：
- `400 {"error": "doc_id 不能为空"}`
- `404 {"error": "文档 X 不存在"}`
- `500 {"error": "请先安装 openpyxl：pip install openpyxl"}`

**前端触发**：知识图谱 Modal 标题栏"导出 Excel"按钮；仅支持单文档视图（通过 `ds-kg-subtitle` 解析当前 doc_id），多文档视图时提示用户切换。

---

#### AI 批量标注知识类型与认知维度

**POST `/api/rag/ds-batch-classify`**

启动异步任务，调用 DeepSeek 对知识点进行布鲁姆分类，批量写入 `knowledge_type` 和 `cognitive_dimension` 字段。

**请求体**：

```json
{
  "doc_id": "农业经济学-导论",
  "kp_ids": [1, 5, 12],
  "overwrite": false
}
```

| 字段 | 必填 | 说明 |
|------|------|------|
| `doc_id` | 与 `kp_ids` 二选一 | 处理该文档下所有符合条件的知识点 |
| `kp_ids` | 与 `doc_id` 二选一 | 指定知识点 ID 列表 |
| `overwrite` | 否（默认 `false`） | `false`=仅补全两个字段均为空的知识点；`true`=全部覆盖 |

**成功响应**：

```json
{"success": true, "task_id": "a1b2c3d4", "total": 23}
```

若无需处理的知识点（`overwrite=false` 且全部已有值），`task_id` 为 `null`，同时返回 `"message"` 说明原因。

---

**GET `/api/rag/ds-classify-tasks/<task_id>`**

轮询批量分类任务状态（建议间隔 1.2 秒）。

```json
{
  "status": "running",
  "progress": 10,
  "total": 23,
  "done_ids": [1, 5, 7, 12, ...],
  "error": null
}
```

`status` 取值：`running` / `done` / `error`

---

**处理流程**：

```
收集待处理知识点（按 overwrite 过滤空值）
        │
        ▼ 每批 15 个
调用 DeepSeek（model=deepseek-chat, temperature=0.1）
        │ 系统提示：布鲁姆分类专家
        │ 用户提示：知识类型定义×4 + 认知维度定义×6（含行动动词）
        │          + 知识点列表（名称 + 内容前200字）
        │ 输出：纯 JSON 数组 [{seq, knowledge_type, cognitive_dimension}]
        │
        ▼
校验合法值（非法值写入 ''，不污染数据）
        │ 知识类型合法值：事实性、概念性、程序性、元认知
        │ 认知维度合法值：记忆、理解、应用、分析、评价、创造
        │
        ▼
UPDATE ds_kps SET knowledge_type=?, cognitive_dimension=? WHERE id=?
        │
        ▼
更新任务进度（progress += batch实际写入数）
```

**Prompt 设计说明**：
- 使用 `seq`（批内序号 1-N）而非 `id` 作为对齐键，避免 ID 泄露和跨批混淆
- `temperature=0.1` 最小化随机性，提高分类一致性
- 内容截断至 200 字，控制 token 消耗

---

#### 知识图谱数据

**GET `/api/rag/ds-graph`**

查询参数（均可重复）：
- `doc_id`：限定文档，不传返回所有 `status='done'` 文档
- `chapter`：按章节名过滤

返回（v1.8.0 起各节点和章节列表均含 `section_name`）：
```json
{
  "nodes": [
    {
      "id": "kp_42",
      "name": "狭义农业与广义农业",
      "chapter": "第一章 农业的内涵与特征",
      "section_name": "第一节 农业内涵",
      "chapter_num": 1,
      "doc_id": "农业经济学-导论",
      "content": "..."
    }
  ],
  "links": [
    {"source": "kp_42", "target": "kp_43", "type": "从属"}
  ],
  "chapters": [
    {"name": "第一章 农业的内涵与特征", "num": 1, "section_name": ""},
    {"name": "第一章 农业的内涵与特征", "num": 1, "section_name": "第一节 农业内涵"},
    {"name": "第一章 农业的内涵与特征", "num": 1, "section_name": "第二节 农业特征"}
  ],
  "total_nodes": 42,
  "total_links": 18
}
```

- `chapters` 列表按 `(chapter_name, section_name)` 去重，旧数据 `section_name` 为空（以章级别条目显示，完全兼容）
- 边的 `type` 对应 `relations_json` 中的关系类型字段。若目标知识点名称在当前查询范围内不存在，该边被忽略。

---

#### AI 出题

**POST `/api/rag/ds-generate`**

请求体：
```json
{
  "doc_ids": ["农业经济学-导论"],
  "chapters": ["第一节农业的内涵与特征"],
  "kp_names": [],
  "prompt": "请根据以下知识图谱内容出题：\n\n{context}\n\n题目要求：{question_list}",
  "question_list": "单选题×10，简答题×3"
}
```

- `chapters` 和 `kp_names` 均可为空（空时使用所选文档的全部知识点）
- `prompt` 支持两个占位符：`{context}`（知识点内容）、`{question_list}`（题目要求）
- `context` 构建：按章节组织，每个知识点附上关联关系，总字符数上限 7000

**context 构建格式示例**：
```
## 第一节农业的内涵与特征

### 知识点：狭义农业与广义农业
狭义农业主要指种植业...
关联关系：从属 → 农业内涵体系；比较 → 广义农业概念

### 知识点：农业的六大基本特征
...
```

**DeepSeek 调用参数**：`model=deepseek-chat`，`temperature=0.7`，`max_tokens=8000`

**返回**：
```json
{
  "success": true,
  "content": "一、单选题\n1. ...",
  "stats": {"kps_used": 8, "context_chars": 3200}
}
```

---

### 5.3 知识图谱可视化 API

**Blueprint 名**：`kg`，无前缀

读取的是 `rag_pipeline/kg.db`（旧版 RAG 管线产生），与 DS 直出模式的 `ds_knowledge.db` 是两个独立数据库。

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/kg` | 知识图谱可视化页面（HTML） |
| GET | `/api/kg/graph` | 节点 + 边数据（D3.js 使用） |
| GET | `/api/kg/chunks` | 指定节点的原文 chunk（分页懒加载） |

**GET `/api/kg/chunks` 查询参数**：

| 参数 | 说明 |
|------|------|
| `doc_id` | 必须，文档 ID |
| `chapter_num` | 可选，章节编号 |
| `section_num` | 可选，小节编号 |
| `concept_name` | 可选，概念名（查其所属章节的 chunks） |
| `limit` | 每页数量，默认 10 |
| `offset` | 跳过前 N 条，默认 0 |

---

## 6. 前端设计

### 6.1 主界面（`templates/index.html`）

单页应用（Vanilla JS + CSS，无前端框架），共 7 个标签页：

| Tab | 名称 | 核心功能 |
|-----|------|----------|
| 1 | 题库管理 | 查询/筛选题目；Quill 富文本编辑器新建/编辑题目；题目预览 |
| 2 | 题库导入 | 上传 Word/TXT 导入；下载导入模板；AI 出题提示词（可编辑/保存/切换） |
| 3 | 组卷设置 | 配置题型数量/分值；选择科目；一键自动组卷 |
| 4 | 试卷管理 | 查看/确认/导出试卷；拖拽调序；单题替换 |
| 5 | 课程设置 | 课程基本信息（打印在 Word 试卷表头） |
| 6 | 题型管理 | 增删自定义题型（内置题型不可删） |
| 7 | 知识图谱 | 上传教材 → 智能解析章/节结构 → 提取知识点 → 图谱可视化（含在线编辑）→ 提示词编辑（可保存/切换）→ 出题 → 导入题库 |

### 6.2 DS 图谱内嵌交互（`templates/index.html`）

知识图谱以弹出 Modal 的形式嵌入在 AI 智能出题标签页，基于 D3.js v7 力导向图实现。

**布局结构（左 → 中 → 右）**：

```
┌──────────┬──────────────────────┬───────────────────┐
│ 左侧筛选  │ D3 图谱画布（flex:1）│ 右侧详情面板 340px │
│ (195px)  │  节点 ── 节点        │ （初始 width:0，  │
│ 章节筛选  │       ╲  ╱          │  点击节点后滑入）  │
│ 搜索框   │        节点          │                   │
│ 关系筛选  │                     │ · 完整名称        │
└──────────┴──────────────────────┴───────────────────┘
```

**节点交互**：

| 交互 | 行为 |
|------|------|
| 鼠标悬停 | 显示 tooltip（名称 + 章节 + 内容摘要 + "点击查看"提示） |
| 单击节点 | 右侧面板滑入，显示完整信息和关联关系列表 |
| 点击画布空白 | 收起右侧面板 |
| ESC（面板开启时） | 收起面板（不关闭 Modal） |
| ESC（面板已关闭时）| 关闭 Modal |

**详情面板状态机**：

```
[关闭] ──点击节点──▶ [只读] ──点击编辑──▶ [编辑中]
                      ◀──点击×/ESC────      │
                      ◀───取消──────────────┤
                      ◀───保存成功──────────┘
```

**详情面板内容区域**（只读 → 编辑对应关系）：

| 区域 | 只读显示 | 编辑控件 |
|------|----------|----------|
| 名称 | `<span>` 文本 | `<input type="text">` |
| 内容 | `<div>` 预览 | `<textarea>` |
| 教学属性 | 彩色标签（重点橙/知识类型蓝/认知维度绿） | 复选框（重点/难点/考点）+ 下拉框（知识类型、认知维度） |
| 关联关系 | 只读列表 | 可增删行（关系类型下拉 + 目标名称输入） |

**教学属性字段**（v1.7.1 新增）：

| 字段 | 控件 | 可选值 |
|------|------|--------|
| 教学要点 | 多选复选框 | 重点、难点、考点 |
| 知识类型 | 下拉单选 | 事实性、概念性、程序性、元认知 |
| 认知维度 | 下拉单选 | 记忆、理解、应用、分析、评价、创造 |

**编辑保存流程**（`_saveKpEdit()`）：
1. 采集面板中 name / content / relation rows / 教学属性（复选框 + 下拉）
2. `PUT /api/rag/ds-kps/<kp_id>`（JSON body，含 `teaching_focus`、`knowledge_type`、`cognitive_dimension`）
3. 成功：同步更新内存节点数据 + D3 SVG，切回只读模式
4. 失败：面板内红色错误提示，保持编辑模式

**切换保护**：编辑未保存时切换节点或关闭面板均弹出 `confirm` 确认对话框。

**Excel 导出按钮**（v1.7.1 新增）：

知识图谱 Modal 标题栏右上角"导出 Excel"按钮，调用 `_exportDsKgXlsx()`：
1. 从 `ds-kg-subtitle` 元素解析当前 `doc_id`（仅支持单文档视图）
2. `GET /api/rag/ds-export-xlsx?doc_id=...` 下载 Blob
3. 通过 `<a download>` 触发浏览器保存，文件名为 `<文档名>_知识点导出.xlsx`

**章节筛选面板**（v1.8.0 更新）：

v1.8.0 起，章节复选框同时展示节名：

```
☑ ⬤ 第一章 绪论
☑ ⬤ 第一章 绪论 › 第一节 研究背景
☑ ⬤ 第一章 绪论 › 第二节 研究目的
☑ ⬤ 第二章 文献综述
```

- 复选框 `value` 编码格式：`章名|||节名`（三竖线分隔，节名可为空）
- 旧数据 `section_name` 为空时只显示章名，完全兼容
- 颜色圆点按章名确定色系，同章内各节保持一致颜色

**筛选逻辑**（`_applyDsKgFilter`，v1.8.0 更新）：

```javascript
// 旧（v1.7.x）：
filteredNodes = _dsKgAllNodes.filter(n => selectedCh.has(n.chapter));

// 新（v1.8.0）：
filteredNodes = _dsKgAllNodes.filter(n => {
    const nodeKey = n.chapter + '|||' + (n.section_name || '');
    return selectedCh.has(nodeKey);
});
```

**AI批量标注区域**（v1.7.2 新增，位于左侧筛选面板底部）：

| 控件 | 说明 |
|------|------|
| ☑ 仅标注当前筛选的节点 | 勾选时只处理章节筛选 + 关键词搜索后可见的节点（默认勾选） |
| ☑ 覆盖已有标注 | 勾选时全部重新标注；不勾选时只补全 `knowledge_type`/`cognitive_dimension` 均为空的节点（默认不勾选） |
| 开始标注 | 调用 `_startBatchClassify()`，提交 POST 请求并启动轮询 |

**`_startBatchClassify()` 执行流程**：
1. 若勾选"仅标注当前筛选"，从 `_dsKgAllNodes` + 当前筛选条件（含 `|||` 复合键）筛出 `kp_ids`
2. `POST /api/rag/ds-batch-classify` → 获取 `task_id`
3. `setInterval(1200ms)` 轮询 `GET /api/rag/ds-classify-tasks/<task_id>`
4. 进度条实时更新；完成后若右侧面板当前节点在 `done_ids` 中，调用 `_refreshDsKgAfterClassify()` 重拉单节点数据刷新属性标签

---

### 6.3 知识图谱页面（`templates/kg.html`）

基于 D3.js v7 实现交互式力导向图。

**节点类型**：

| 类型 | 颜色 | 说明 |
|------|------|------|
| `doc` | 深蓝 | 文档根节点 |
| `chapter` | 蓝绿 | 章节节点 |
| `section` | 绿 | 小节节点 |
| `concept` | 橙 | 概念/知识点（来自旧版 KG 数据库） |

**边类型**：
- `structural`：文档/章节/小节层级关系
- `has_concept`：章节包含概念
- 语义关系：`is_a`、`contrasts_with`、`depends_on`、`leads_to`

**交互功能**：
- 鼠标悬停显示节点详情（概念描述/chunk 数量）
- 点击节点展开原文 chunk（分页加载）
- 支持按 `doc_id` 过滤（URL 参数）

### 6.3 图片存储流程

```
用户在 Quill 编辑器中粘贴/上传图片
        │ POST /api/images/upload
        ▼
服务器保存到 uploads/images/{image_id}.png
        │ 返回 {image_id, url}
        ▼
Quill 将 <img src="/api/images/img_xxx"> 插入富文本 HTML
        │
        ▼
保存/更新题目时，_associate_images_in_html()
扫描 HTML 中的 /api/images/ URL，将 image_id 与 question_id 绑定
        │
        ▼
删除题目时，delete_question_images() 删除磁盘文件 + DB 记录
```

图片 ID 在上传时生成（`img_` + UUID 前 8 位），与题目解耦，支持先上传后保存题目的工作流。

### 6.4 Word 导入格式

支持两种格式：

**`.docx` 格式**（推荐）：使用 Word 样式标记题型，`parse_docx_with_rich_content()` 解析，支持嵌入图片和表格。

**`.txt` 格式**：结构化文本，`parse_question_template()` 解析：
```
【单选】题目内容...
A. 选项A
B. 选项B
答案：A
解析：...
```

---

## 7. AI 知识点提取流程详解

### 7.1 完整数据流

```
用户上传 .md 教材文件
        │
        ▼
POST /api/rag/ds-upload
        ├─ 文件保存到 rag_uploads/
        ├─ 清理 OCR 噪声（_clean_ocr_md）
        ├─ 提取所有标题（_extract_all_headings）
        │       └─ 返回 [{level:1-6, text, pos}, ...] 按位置排序
        ├─ DeepSeek Layer 0 标题清洗（_clean_headings_with_ai）
        │       ├─ 发送前60个标题，AI 判断层级语义 + 过滤无效标题
        │       ├─ 返回 {chapter_level, section_level, leaf_level, non_content_texts}
        │       └─ 失败/超时 → 降级到本地 _analyze_doc_structure（2级）
        ├─ 多层级切分（_parse_md_multilevel）
        │       ├─ 按 chapter_level（L1）→ section_level（L2）→ leaf_level（L3）三级切分
        │       ├─ 过滤 non_content_texts 和 _NON_CONTENT_KEYWORDS 中的标题
        │       └─ 两级文档（leaf=section）行为与旧版一致
        └─ 写入 ds_docs（status='uploaded'）+ ds_chapters（含 parent_chapter_* + section_name）
                │
                ▼
POST /api/rag/ds-extract/<doc_id>
        ├─ 创建 task_id，写入 _ds_tasks 内存字典
        ├─ 启动后台线程 _worker()
        │        │
        │        ├─ Layer 1：架构分析
        │        │     查询去重父章列表 → 调用 DeepSeek → 存 architecture_json
        │        │
        │        ├─ Layer 2：并发知识点提取（ThreadPoolExecutor）
        │        │     循环遍历每个节（ds_chapters 行）
        │        │     ├─ 截断超长文本（8000字上限）
        │        │     ├─ 从架构中取父章主题（parent_chapter_num 对应）
        │        │     ├─ 调用 DeepSeek API（节文本 + 架构上下文）
        │        │     ├─ 解析 JSON → knowledge_points
        │        │     ├─ 写入 ds_kps（chapter_name = parent_chapter_name）
        │        │     └─ 检查 pause_requested
        │
        └─ 立即返回 {task_id}
                │
                ▼（前端轮询）
GET /api/rag/ds-tasks/<task_id>
        返回 {status, progress, message, kp_count}
                │
                ▼（提取完成）
GET /api/rag/ds-docs/<doc_id>/kps
        返回父章列表（去重）+ 知识点列表（含关系）
                │
                ├─ 用户在图谱中点击知识点节点
                │       GET /api/rag/ds-kps/<kp_id>  ← 可选（当前由前端从全量数据中取）
                │       PUT /api/rag/ds-kps/<kp_id>  ← 用户编辑后保存
                │
                ▼（选择范围出题）
POST /api/rag/ds-generate
        ├─ 按 doc_ids + chapters + kp_names 查询 ds_kps
        ├─ 构建知识点 context（按章节组织，≤7000字）
        ├─ 替换 prompt 占位符
        └─ 调用 DeepSeek API 生成题目文本
                │
                ▼（前端复制/编辑后）
POST /api/questions/import（.txt 格式导入）
        写入 exam_system.db
```

### 7.2 DeepSeek API 调用参数汇总

| 调用场景 | temperature | max_tokens | 说明 |
|----------|-------------|------------|------|
| **Layer 0：标题清洗**（v1.8.0 新增） | 0 | 400 | 判断层级语义 + 过滤无效标题，最大确定性 |
| 章节级别检测（OCR降级） | 0 | 120 | 确定性判断，低温度 |
| 知识点提取 | 0.3 | 3000 | 结构化输出，低创造性 |
| AI 批量分类（知识类型/认知维度） | 0.1 | — | 最小随机性，提高一致性 |
| 出题生成 | 0.7 | 8000 | 需要创造性，中温度 |

所有调用均使用 `openai` SDK，`base_url='https://api.deepseek.com'`。

### 7.3 错误处理策略

- **单章提取失败**：记录错误信息到 `_ds_tasks[task_id]['message']`，跳过该章继续处理后续章节（不中断整个任务）
- **API Key 缺失**：立即终止任务，更新 `ds_docs.status='error'`
- **JSON 解析失败**：同单章提取失败，跳过该章
- **任务意外崩溃**：顶层 `try/except` 捕获，写入 `status='error'` 和错误信息

---

## 8. 配置与环境变量

### 8.1 `.env` 文件

```env
DEEPSEEK_API_KEY=sk-xxxxxxxxxxxxxxxx
```

位于数据根目录（开发模式下为项目根目录），通过 `python-dotenv` 的 `dotenv_values()` 读取（不修改进程环境变量，避免污染），不入 git。

**读取优先级**（`_get_deepseek_key()`）：
1. `.env` 文件中的 `DEEPSEEK_API_KEY`
2. `.env` 文件中的 `DEEPSEEK_TOKEN`（旧版字段名）
3. 进程环境变量 `DEEPSEEK_API_KEY`
4. 进程环境变量 `DEEPSEEK_TOKEN`

### 8.2 环境变量

| 变量 | 说明 |
|------|------|
| `EXAM_DATA_DIR` | 数据根目录（打包版由 launcher.py 设置） |
| `DATABASE_URL` | SQLAlchemy 连接串（默认自动推导） |
| `SECRET_KEY` | Flask session 密钥（开发版有默认值） |
| `PORT` | 监听端口（server.py 读取，默认 5000） |

### 8.3 Flask 配置类（`config.py`）

| 配置类 | 用途 | DATABASE |
|--------|------|----------|
| `DevelopmentConfig` | 本地开发，`DEBUG=True` | `exam_system.db`（项目根） |
| `ProductionConfig` | 打包版，`DEBUG=False` | `exam_system.db`（用户数据目录） |
| `TestingConfig` | pytest，`TESTING=True` | 内存 SQLite（`:memory:`） |

---

## 9. 启动方式

### 9.1 开发模式（推荐用于调试）

```bash
cd D:\code\试卷\试卷生成-simple
.\venv\Scripts\activate          # Windows
# source venv/bin/activate       # macOS/Linux

pip install -r requirements.txt

python server.py
# → http://localhost:5000
```

### 9.2 首次配置 API Key

**方式一**：在项目根目录创建 `.env` 文件：
```
DEEPSEEK_API_KEY=sk-xxxxxxxxxxxxxxxx
```

**方式二**：启动后在前端 "AI 智能出题" → "API 配置" 输入框填写，点击保存（自动写入 `.env`）。

---

## 10. 打包部署（macOS DMG）

详见 `打包说明.md`，核心流程：

```bash
pyinstaller exam_system.spec    # 生成 .app
create-dmg ...                  # 打包为 .dmg
```

**关键配置**：

| 项目 | 说明 |
|------|------|
| 入口文件 | `launcher.py` |
| 数据目录 | `~/Library/Application Support/试题管理系统/` |
| 端口探测 | 从 5050 开始，最多尝试 30 个连续端口 |
| 浏览器打开 | 延迟 1.8 秒后调用 `webbrowser.open(url)` |

**`launcher.py` 启动顺序**（顺序严格，不可乱）：
1. `_data_dir()` 确定并创建用户数据目录及子目录
2. `_setup_env()` 写入 `EXAM_DATA_DIR`、`DATABASE_URL` 环境变量
3. **延迟导入** `from app.factory import create_app`（确保环境变量先于模块加载）
4. `create_app('production')` 初始化 Flask 应用
5. 启动浏览器线程（daemon）
6. `flask_app.run(threaded=True)` 阻塞主线程

**打包注意事项**：
- `rag_routes.py` 和 `kg_routes.py` 以 `try/except` 注册，打包时可按需排除
- `templates/` 目录必须加入 spec 的 `datas` 列表
- `ds_knowledge.db` 不打包进 `.app`，运行时自动创建于用户数据目录

---

## 11. 测试体系

### 11.1 框架与策略

- **工具**：`pytest`
- **数据库隔离**：`TestingConfig` 使用内存 SQLite（`sqlite://`），每个测试函数通过 `conftest.py` 的 fixture 获得独立数据库
- **覆盖范围**：API 端点、ORM 模型、业务逻辑、边界条件

### 11.2 测试文件说明

| 文件 | 测试内容 |
|------|----------|
| `test_question_api.py` | 题目 CRUD、过滤查询 |
| `test_exam_api.py` | 组卷创建/删除/导出、题目增删/替换/排序 |
| `test_question_types.py` | 题型创建/更新/删除（含内置题型保护） |
| `test_models.py` | ORM 模型字段、`to_dict()` 序列化 |
| `test_business_logic.py` | 重复题目过滤、分值计算、试卷确认锁定 |
| `test_bilingual.py` | 双语题目（`language='both'`）处理 |
| `test_course_settings.py` | 课程配置读写 |
| `test_edge_cases.py` | 空值/越界/格式异常情况 |
| `test_batch_delete.py` | 批量删除（含关联清理） |
| `test_usage_management.py` | `is_used` 状态管理 |

### 11.3 运行测试

```bash
# 运行全部测试
pytest tests/ -v

# 运行单个文件
pytest tests/test_question_api.py -v

# 查看覆盖率（需安装 pytest-cov）
pytest tests/ --cov=app --cov-report=term-missing
```

---

## 12. 已知问题与修复记录

### Feature #1：知识点教学属性 + Excel 导出（v1.7.1，2026-03-21）

**背景**：用户需要将知识图谱导出为平台通用的《批量导入知识点模板》格式（.xlsx），同时希望在图谱中标注教学属性（教学要点/知识类型/认知维度）。

**实现内容**：

1. **数据库**：`ds_kps` 表新增 3 列（旧库自动迁移）：

   | 列名 | 类型 | 说明 |
   |------|------|------|
   | `teaching_focus` | TEXT | 分号分隔，如 `重点;难点` |
   | `knowledge_type` | TEXT | 布鲁姆知识维度：事实性/概念性/程序性/元认知 |
   | `cognitive_dimension` | TEXT | 布鲁姆认知维度：记忆/理解/应用/分析/评价/创造 |

2. **API 更新**：
   - `GET /api/rag/ds-kps/<id>`：响应新增三字段
   - `PUT /api/rag/ds-kps/<id>`：请求体支持三字段（不传则写入 `''`）
   - `GET /api/rag/ds-kp-list`（知识点列表）：同步返回三字段
   - **新增** `GET /api/rag/ds-export-xlsx?doc_id=`：导出 Excel 文件

3. **前端**：
   - 详情面板新增"教学属性"区域（只读彩色标签 ↔ 编辑表单）
   - 知识图谱 Modal 标题栏新增"导出 Excel"按钮

**兼容性**：旧库启动时自动执行 `ALTER TABLE ds_kps ADD COLUMN ...`，无需手动迁移。

---

### Feature #2：AI 批量标注知识类型与认知维度（v1.7.2，2026-03-21）

**背景**：知识点提取后，`knowledge_type`（布鲁姆知识维度）和 `cognitive_dimension`（布鲁姆认知维度）字段均为空，需要手动逐条编辑，效率低。

**实现内容**：

1. **新增后端任务**（`app/rag_routes.py`）：
   - `POST /api/rag/ds-batch-classify`：接收 `doc_id` 或 `kp_ids`，启动后台线程
   - `GET /api/rag/ds-classify-tasks/<task_id>`：轮询状态，返回 `{status, progress, total, done_ids}`
   - 全局字典 `_classify_tasks` 存储任务状态（同 `_ds_tasks` 模式）

2. **Prompt 设计**：
   - `_CLASSIFY_SYSTEM`：布鲁姆分类专家角色
   - `_CLASSIFY_PROMPT`：内嵌完整的知识类型×4 + 认知维度×6 定义（含行动动词），输出纯 JSON
   - `temperature=0.1`，每批 15 个知识点（名称 + 内容前 200 字）

3. **安全校验**（`_classify_batch()`）：
   - 返回值不在合法集合内时写入 `''`，不污染数据
   - 使用批内 `seq`（1-N）对齐结果，防止跨批 ID 混淆

4. **前端**（左侧筛选面板底部"AI批量标注"区域）：
   - 两个选项：仅处理筛选节点 / 覆盖已有标注
   - 进度条实时更新（轮询间隔 1.2 秒）
   - 完成后自动刷新右侧面板当前节点的属性标签

**无数据库结构变更**：复用 v1.7.1 已添加的 `knowledge_type`、`cognitive_dimension` 列。

---

### Bug Fix #1：Excel 导出三项修复（v1.7.2，2026-03-21）

**背景**：v1.7.1 初版 Excel 导出存在三个问题：①未保留模板前两行；②L4知识点全部堆在末尾，未与L3小节交叉排列；③非内容章节（学习目标、小结等）混入导出，主要参考文献无独立存储。

**修复内容**：

1. **模板保留前两行**：由程序自建表头改为以 `批量导入知识点模板.xlsx` 为基础，用 `delete_rows(3, max_row-2)` 清空第3行起的旧数据，保持第1-2行（表头与说明）不变。

2. **L3/L4 交叉排列**：重构层级构建逻辑，以 `parent_chapter_num` 为分组键，每组按"L2行 → 该组所有L3行 → 该组所有L4知识点行"顺序输出，而非将全部L4堆在最末。

3. **非内容节过滤 + 参考文献独立存储**：
   - 新增 `_NON_CONTENT_KEYWORDS` 和 `_is_non_content()` 函数，导出时跳过学习目标/小结/关键词/复习思考题/思考题/练习题等章节
   - 主要参考文献不参与导出，内容写入新增 `ds_doc_refs` 表（含自动迁移：启动时将 `ds_chapters` 中已有的参考文献行迁移过去）

**涉及代码**（`app/rag_routes.py`）：
- `_ensure_tables()`：新增 `ds_doc_refs` 建表 + 自动迁移逻辑
- `_NON_CONTENT_KEYWORDS`、`_is_non_content()`：新增于文件顶部
- `ds_export_xlsx()`：重写层级构建与输出逻辑

---

### Feature #3：多层级标题提取与 DeepSeek Layer 0 清洗（v1.8.0，2026-03-21）

**背景**：旧版仅解析 h2/h3 两级标题，三级文档（h2 章 / h3 节 / h4 小节）的 h4 内容被丢弃；同时 OCR 产生的页码（`## 23`）等无效标题混入切分，导致粒度粗、归属不精确。

**实现内容**：

1. **新增 `_extract_all_headings(text)`**（`rag_routes.py`）：
   - 提取文档所有 h1-h6 标题，返回 `[{level, text, pos}]` 按位置排序

2. **新增 `_clean_headings_with_ai(headings, subject, client)`**（`rag_routes.py`）：
   - 将前 60 个标题格式化为 `"H2: 标题文本"` 发送 DeepSeek（`temperature=0`，`max_tokens=400`）
   - AI 判断各级别语义（章/节/叶子）并识别页码、图注、小结等无效标题
   - 返回 `{chapter_level, section_level, leaf_level, non_content_texts: set}`
   - 任何失败均返回 `None`，调用方降级到本地分析

3. **新增 `_parse_md_multilevel(text, hierarchy)`**（`rag_routes.py`）：
   - 按最多三级（L1章 / L2节 / L3小节）切分，`leaf_level` 控制最小提取单元
   - 过滤 `non_content_texts`（AI 识别）和 `_NON_CONTENT_KEYWORDS`（本地关键词）
   - `leaf_level == section_level` 时行为与旧版 2 级解析完全一致（无额外开销）
   - 返回条目含 `section_name` 字段（三级文档的小节名，两级文档为 `''`）

4. **数据库迁移**（`_init_ds_db()`）：
   - `ds_chapters` 新增 `section_name TEXT DEFAULT ''`
   - `ds_kps` 新增 `section_name TEXT DEFAULT ''`（存 L2 节名，用于图谱筛选"章名 > 节名"）

5. **`ds_upload` 端点更新**：
   - 新流程：`_extract_all_headings` → `_clean_headings_with_ai` → `_parse_md_multilevel`
   - `ds_chapters` INSERT 新增 `section_name` 字段

6. **`ds_kps` 写入更新**（`_extract_one`）：
   - `section_name` 存 `ch['chapter_name']`（L2 节名）

7. **`ds_graph` 端点更新**：
   - 节点新增 `section_name` 字段
   - `chapters` 列表按 `(chapter_name, section_name)` 去重，旧数据兼容

8. **`ds_export_xlsx` 层级重写**：
   - 从 `ds_kps.section_name` 重建层级（不再依赖 `ds_chapters`）
   - 两种排列：有节名时 L3(节)+L4(KP)；无节名时 KP 直接在 L3

9. **前端 `index.html` 更新**：
   - `_buildDsKgFilterPanel`：复选框显示"章名 › 节名"，`value` 编码为 `章名|||节名`
   - `_applyDsKgFilter` / `_startBatchClassify`：改用 `章名|||节名` 联合键过滤

**向后兼容**：旧数据 `section_name` 均为 `''`，章节筛选仅显示章名条目，行为与旧版一致。

---

### Feature #4：Layer 0 节点列表替代 leaf_level（v1.9.0，2026-03-21）

**背景**：v1.8.0 的 Layer 0 返回 `leaf_level` 整数，解析器按该层级盲目切分，导致同一文档不同节粒度不一致（有 H6 的节切得太细，无 H6 的节整节合并成大块）。测试验证 DeepSeek 具备直接识别"合适的提取节点列表"的能力，准确率与人工判断高度一致。

**实现内容**：

1. **Layer 0 返回格式变更**：去除 `leaf_level`，新增 `extraction_nodes`（有序标题文本列表）：
   ```json
   {
     "chapter_level": 2, "section_level": 3,
     "non_content_texts": [...],
     "extraction_nodes": ["一、 农业经营制度的含义", "二、 农业经营制度变迁", ...]
   }
   ```

2. **`_clean_headings_with_ai` prompt 重写**：新增 `extraction_nodes` 粒度原则（100-500字/节点，H3有H4时选H4），三阶段后处理确保稳定性：
   - 阶段1（展开）：全为 section_level 时自动展开为子节点，跳过 non_content 边界
   - 阶段2（去重）：父子同在列表时删除父节点
   - 阶段3（补全）：对被删父节点补全 DeepSeek 遗漏的子节点，按文档顺序重排

3. **新增 `_parse_md_by_nodes(text, hierarchy)`**：按 `extraction_nodes` 集合定位标题切分内容，追踪章/节上下文（三处修正：节点自身不更新 section 上下文、同级兄弟不互为父节点、non_content 标题不污染章名）。

4. **`ds_upload` 端点**：有 `extraction_nodes` 时调用 `_parse_md_by_nodes`，否则降级到 `_parse_md_multilevel`。

**测试结果**（教材型文档 `知识点提取测试用例.md`）：8/8 节点全部命中，误标标题 `这一框架的内涵是：` 不出现在任何章节名字段。

---

### Feature #5：PPT 文档类型自动检测与过渡页源头过滤（v1.9.0，2026-03-21）

**背景**：PPT 转换文档中，封面/过渡/致谢幻灯片的标题若进入 `extraction_nodes`，将产生空内容节点，浪费 KP 提取 API 调用。简单文本长度过滤（< 80 字）会误伤教材中短但有意义的定义型节点。

**实现内容**：

1. **新增 `_detect_doc_type(headings)`**：
   - 判据：H1 出现 ≥3 次 + H1 占比 ≥20% + 层级深度 ≤3 + 最深层级 ≤3 → `'ppt'`；其余 → `'textbook'`
   - 对两份测试文档验证准确（PPT: H1=19/49=39%；教材: H1=1/54=2%）

2. **`_clean_headings_with_ai` PPT 专用 prompt**：
   - 明确限制 `non_content_texts` 和 `extraction_nodes` 只放 H1 级别标题，H2 子项不进入任何列表
   - `non_content_texts` 过滤标准：课程封面/过渡套语/纯问句(?)H1/无关内容/重复H1
   - 强调：描述知识内容的 H1（即使带疑问词但不以?结尾）应放入 `extraction_nodes`
   - 解析后强制覆盖 `chapter_level=1, section_level=2`

3. **`_parse_md_by_nodes` 边界修复**：将内容终止条件从 `h['level'] <= section_level` 改为 `h['level'] <= node_h['level']`，确保 PPT H1 节点的内容涵盖其下所有 H2 子项（不在第一个 H2 处截断）。对教材型文档无回归影响。

**测试结果**（PPT 文档 `知识点提取测试用例2-无明显章节标识.md`）：
- 11 个实质内容节点，0 个薄节点，每节 199–5866 字
- 封面/过渡页（农业项目管理、什么是项目启动?、辞职回乡种地...等）正确进入 `non_content_texts`
- 教材型回归：8/8 节点全部通过

**新增测试脚本**：`test_ppt_layer0.py`（文档类型检测 + Layer 0 过滤质量 + 切分预览 + 教材回归四个部分）。

---

### Bug #1：Markdown 文档标题层级误判（v1.6.3 彻底重构）

**现象（v1.6.2 之前）**：以 `##` 为最高层标题的 `.md` 文件上传后，章节数为 1，AI 无法提取知识点。

**根因（v1.6.2 的局限）**：v1.6.2 引入 `_auto_detect_heading_level()` 查找"最高层标题"，但若文档有一个 `#` 书名（仅出现 1 次），仍会把 `#` 判为章级别，导致整本书变成一章；且该函数只检测章级别，忽略了更细粒度的节级别。

**v1.6.3 重构方案**（`app/rag_routes.py`，2026-03-18）：

用 `_analyze_doc_structure()` 替换 `_auto_detect_heading_level()`：

```python
def _analyze_doc_structure(text: str) -> tuple:
    """
    返回 (chapter_level: int, section_level: int | None)

    chapter_level：出现次数 ≥ 2 的最高级别标题
    （仅出现 1 次的顶层标题视为文档总标题，跳过）
    section_level：比章更深的下一个存在的标题级别
    """
```

同时将章节存储粒度从"章"细化为"节"：

- 用 `_parse_md_to_sections()` 替换 `_parse_md_to_chapters()`
- `ds_chapters` 表新增 `parent_chapter_num`、`parent_chapter_name` 字段
- 知识点提取以**节**为单元，`ds_kps.chapter_name` 存父章名（用于 UI 聚合）

**典型场景对比**：

| 文档结构 | v1.6.2 行为 | v1.6.3 行为 |
|---------|-------------|-------------|
| `# 书名`（1次）+ `## 第一章`×N | 判章级别=1，整书1章 ❌ | 判章级别=2，节级别=None，N章 ✅ |
| `## 第一章`×N + `### 第一节`×M | 判章级别=2，N章（整章提取） | 判章级别=2，节级别=3，M节分别提取 ✅ |
| `# 绪论`（1次）+ `## 第一章`×N + `### 节`×M | 判章级别=1，整书1章 ❌ | 判章级别=2，节级别=3，M节分别提取 ✅ |

**向后兼容**：旧库自动迁移（`ALTER TABLE` 追加新列），已有知识点数据不受影响。

---

### Bug #2：旧版 rag_pipeline 第 15 章未被识别

**现象**：OCR 产生的第 15 章使用单 `#` 作为标题，旧版正则 `^## ` 无法匹配。

**修复**：正则从 `^## ` 改为 `^#{1,2} `，同时兼容 1 级和 2 级标题。

---

### Bug #3：Windows GBK 终端 UnicodeEncodeError（旧版 rag_pipeline）

**现象**：Windows 默认 GBK 编码，打印 Unicode 符号（✓、✗）时报错。

**修复**：将所有 Unicode 符号替换为 ASCII 等价（`[OK]`、`[FAIL]` 等），并在批处理文件中使用 `python -X utf8` 强制 UTF-8 模式。

---

### Feature #1：知识点在线编辑（v1.6.4，2026-03-18）

**背景**：知识图谱节点只能悬停预览（内容被截断至 220 字），无法查看完整信息，更无法修正 AI 提取的错误。

**实现**：

**后端**（`app/rag_routes.py`）：
- 新增 `GET /api/rag/ds-kps/<kp_id>` — 返回完整知识点信息
- 新增 `PUT /api/rag/ds-kps/<kp_id>` — 更新 name/content/relations，自动过滤空 target 条目

**前端**（`app/templates/index.html`）：
- 图谱弹窗布局改为三栏：左侧筛选面板 + 中间 D3 画布 + 右侧详情面板（340px，CSS transition 滑入）
- 节点新增 `.on('click')` 事件，调用 `_openKpPanel(d)` 展开详情面板
- 详情面板含两种状态（只读/编辑），通过 `_enterKpEdit()` / `_cancelKpEdit()` 切换
- `_saveKpEdit()` 采集表单数据，`PUT` 保存后同步更新 `_dsKgAllNodes` 内存数组和 D3 SVG 文字
- ESC 键优先收起面板，再次按关闭 Modal

**关键全局变量**：

| 变量 | 类型 | 用途 |
|------|------|------|
| `_kpPanelData` | Object/null | 当前显示的 D3 节点 datum |
| `_kpPanelEditing` | Boolean | 是否处于编辑模式 |
| `_KP_REL_TYPES` | Array | 关系类型枚举（从属/比较/并列/交叉/前提） |

---

### Feature #2：提示词模板命名保存与加载（v1.7.0，2026-03-18）

**背景**：用户修改提示词后，刷新页面或切换语言模式会导致自定义内容丢失；且两个提示词入口（知识图谱出题页、题库导入页）均无保存机制。

**实现**：

**前端**（`app/templates/index.html`）：

*知识图谱页（Tab 7）*：
- 提示词编辑区新增"切换提示词"选择器（`#rag-prompt-selector`），分两个分组展示：`默认模板`（中文/双语/English）和 `我的提示词`（已保存自定义项）
- 新增「保存当前」按钮，调用 `saveRagPromptTemplate()`
- 新增「删除」按钮（`#rag-delete-prompt-btn`），仅在选中自定义提示词时显示；UI 层 + 函数层双重保护，默认模板不可删除

*题库导入页（Tab 1）*：
- 原 `prompt-mode-select` 下拉框替换为 `#import-prompt-selector` 分组选择器（原选择器隐藏保留供内部逻辑使用）
- `prompt-display` 文本框由 `readonly` 改为可编辑（`background:#fff`）
- 同样新增「保存当前」和「删除」按钮，交互逻辑与知识图谱页一致

**共享存储模块**（同一 `<script>` 块）：

| 函数 | 说明 |
|------|------|
| `_getSavedPrompts(key)` | 从 localStorage 读取已保存列表 |
| `_setSavedPrompts(key, arr)` | 写回 localStorage |
| `_savePromptToStore(key, name, content)` | 追加新条目，返回 `id` |
| `_overwritePromptInStore(key, id, content)` | 原地更新已有条目 |
| `_resolveAndSavePrompt(key, name, content)` | 重名处理：弹出覆盖确认 → 拒绝时自动追加今日日期后缀 → 仍冲突则要求手动改名 |
| `_deletePromptFromStore(key, id)` | 按 id 删除条目 |

**存储键**：
- `saved_prompts_kg` — 知识图谱出题页
- `saved_prompts_import` — 题库导入页

**存储条目格式**：
```json
{
  "id": "custom_1742300000000",
  "name": "农业经济学专用",
  "content": "你是一位...",
  "createdAt": "2026-03-18T10:00:00.000Z"
}
```

**重名处理流程**：
```
用户输入名称
    │
    ├─ 名称不存在 → 直接保存（isNew=true）
    │
    └─ 名称已存在
           │
           ├─ confirm("是否覆盖？")
           │      ├─ 确定 → 原地更新（isNew=false）
           │      └─ 取消 → 自动改名为 "<name> YYYY-MM-DD"
           │                    ├─ 新名不存在 → 保存
           │                    └─ 新名也冲突 → prompt() 要求再次手动输入
           │                                      ├─ 输入不重复 → 保存
           │                                      └─ 仍重复 → confirm 是否覆盖
```

保存成功后，状态栏（`#rag-prompt-save-status` / `#import-prompt-save-status`）显示 `✓ 已保存为"xxx"` 或 `✓ 已覆盖"xxx"`，3 秒后自动清除。

---

## 13. 依赖清单

### 生产依赖（`requirements.txt`）

| 包 | 用途 |
|----|------|
| `flask` | Web 框架，路由/Blueprint/模板 |
| `flask-sqlalchemy` | ORM，管理 `exam_system.db` |
| `python-docx` | Word 文档读取（导入）和生成（导出） |
| `openai>=1.0` | DeepSeek API 调用（OpenAI 兼容协议） |
| `python-dotenv>=1.0` | `.env` 文件读取 |

### 开发/测试依赖

| 包 | 用途 |
|----|------|
| `pytest` | 测试框架 |

### 前端库（CDN，无需安装）

| 库 | 版本 | 用途 |
|----|------|------|
| D3.js | v7 | 知识图谱力导向图 |
| Quill | 1.x | 富文本题目编辑器 |

### 技术选型说明

| 选择 | 原因 |
|------|------|
| Flask（非 FastAPI） | 同步 I/O 足够，生态成熟，PyInstaller 兼容性好 |
| SQLAlchemy ORM | 类型安全，DB 迁移方便，测试用内存库 |
| 原生 sqlite3（DS 库） | 与 Flask ORM 解耦，轻量，无额外依赖 |
| openai SDK + DeepSeek base_url | DeepSeek API 完全兼容 OpenAI 协议 |
| Vanilla JS（无前端框架） | 零构建依赖，打包时无需 Node，部署极简 |
| D3.js v7 | 力导向图生态最成熟，支持高度自定义 |
| `threading.Thread`（非 asyncio） | Flask 同步模式下后台任务最简单方案 |

---

*本文档基于 `ds-text-only` 分支，2026-03-21 更新至 v1.9.0，覆盖 Layer 0 节点列表替代 leaf_level（v1.9.0 Feature #4）和 PPT 文档类型自动检测与过渡页源头过滤（v1.9.0 Feature #5）。*
