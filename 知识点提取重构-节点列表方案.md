# 知识点提取重构方案 v3.0 — 节点列表替代 leaf_level

> 文档状态：**已实施完成**
> 编写日期：2026-03-21
> 最后更新：2026-03-21（补充 §十：PPT 过渡页过滤方案）
> 涉及文件：`app/rag_routes.py`（唯一修改文件）

---

## 一、问题总结

### 现有方案的根本缺陷

现有 Layer 0 让 DeepSeek 返回一个 `leaf_level` 整数，解析器按该层级盲目切分：

```
leaf_level = 6
→ 有 H6 的节：切成 H6 小块（太细，"1. 完善土地流转服务体系" 独立成节）
→ 无 H6 的节：leaf_matches=[] → 整节合并成一个大块（太粗）
→ 同一文档，不同节粒度完全不一致
```

### 测试验证的结论

通过对比测试，DeepSeek 具备直接识别"合适的提取节点列表"的能力，准确率与人工判断高度一致（8个节点中7个完全一致，唯一分歧是一个定义性短节）。应直接让 DeepSeek 返回节点名称列表，代码按名称定位内容切分。

---

## 二、新方案设计

### 2.1 Layer 0 返回格式变更

**旧格式**：
```json
{
  "chapter_level": 2,
  "section_level": 3,
  "leaf_level": 6,
  "non_content_texts": ["小结", "关键词", "这一框架的内涵是："]
}
```

**新格式**：
```json
{
  "chapter_level": 2,
  "section_level": 3,
  "non_content_texts": ["小结", "关键词", "这一框架的内涵是："],
  "extraction_nodes": [
    "一、 农业经营制度的含义",
    "二、 农业经营制度变迁",
    "一、 农业经营主体",
    "二、 农业经营形式",
    "三、 农业经营制度的运行机制",
    "一、 新型农业经营体系",
    "二、 农业产业融合",
    "三、 农业社会化服务体系"
  ]
}
```

`extraction_nodes` 是**有序标题文本列表**，每个条目对应文档中一个标题行的文本，代码用它定位内容边界。

---

## 三、新 Prompt 设计

### 3.1 `_clean_headings_with_ai` 的 prompt 替换

```python
prompt = (
    '你是教材文档结构分析专家。以下是一份教材的所有 Markdown 标题（含层级）：\n\n'
    f'【学科/科目】：{subject or "（未指定）"}\n\n'
    '【标题列表】：\n'
    f'{heading_list}\n\n'
    '请完成以下分析，直接输出纯 JSON（不加代码块）：\n'
    '{\n'
    '  "chapter_level": 章标题的H级别数字,\n'
    '  "section_level": 节标题的H级别数字（若无节则与chapter_level相同）,\n'
    '  "non_content_texts": ["不是章节内容的标题：页码/结构性标题/正文误标等"],\n'
    '  "extraction_nodes": ["适合提取知识点的标题文本，按文档顺序"]\n'
    '}\n\n'
    'extraction_nodes 选取原则：\n'
    '- 粒度目标：每个节点对应1-3个知识点的内容（通常100-500字）\n'
    '- 若某节（H3）包含多个子节（H4），应以H4子节为节点，而非选整个H3节\n'
    '- H5/H6 的细碎列表项（如"1. 完善土地流转..."）通常太细，不单独列为节点\n'
    '- 若某节没有子节，该节标题本身就是节点\n'
    '- non_content_texts 中的标题不放入 extraction_nodes\n'
    '- 标题文本请原样复制，不要修改\n\n'
    'non_content_texts 包含：\n'
    '- 结构性标题：小结/关键词/复习思考题/参考文献/学习目标/章/编 等\n'
    '- 误标的正文句子：明显是句子、定义引导语（如"这一框架的内涵是："）\n'
    '- 页码、图表编号等非章节标题'
)
```

**调用参数**：`temperature=0`，`max_tokens=800`

### 3.2 校验与后处理逻辑（实际实施版）

```python
chapter_level = int(parsed.get('chapter_level', 2))
section_level_val = parsed.get('section_level')
section_level = int(section_level_val) if section_level_val else chapter_level
if section_level < chapter_level:
    section_level = chapter_level

non_texts = set(str(t).strip() for t in parsed.get('non_content_texts', []) if t)
extraction_nodes = [str(n).strip() for n in parsed.get('extraction_nodes', []) if n]

if not extraction_nodes:
    return None  # 降级到本地分析

# ── 三阶段后处理（见 §9.2 差异1）────────────────────────────────────
child_level = section_level + 1

def _collect_children(node_text):
    """收集某节点的 child_level 子节点，跳过 non_content 标题（不作边界）"""
    ...

# 阶段1：全为 section_level 节点时，展开为 child_level 子节点
if all(heading_level_map.get(n,0) == section_level for n in extraction_nodes):
    extraction_nodes = [expanded children ...]

# 阶段2：去除父子重复（H3 有 H4 子节点在列表中，删 H3）
extraction_nodes = [filtered ...]

# 阶段3：补全被删父节点下遗漏的子节点，按文档顺序重排
extraction_nodes = [completed and reordered ...]

return {
    'chapter_level':     chapter_level,
    'section_level':     section_level,
    'non_content_texts': non_texts,
    'extraction_nodes':  extraction_nodes,
}
```

---

## 四、新解析函数 `_parse_md_by_nodes`

### 4.1 函数签名

```python
def _parse_md_by_nodes(text: str, hierarchy: dict) -> list:
    """
    按 extraction_nodes 列表切分文档，返回叶子节点列表。
    hierarchy 必须包含 extraction_nodes 字段（来自 Layer 0）。
    """
```

### 4.2 算法步骤（实际实施版）

```
Step 1: 过滤 non_content_texts（与 _parse_md_multilevel 相同逻辑）
  - 章级别（<=chapter_level）的假标题：只删标题行，保留内容
  - 节级别（>chapter_level）的结构性标题：删标题行 + 内容

Step 2: 提取过滤后文本中所有标题及位置
  all_headings = [{'level': int, 'text': str, 'start': int, 'end': int}, ...]

Step 3: 建立节点索引
  node_set = set(extraction_nodes)
  对 all_headings 中每个 heading，标记 is_node = heading['text'] in node_set

Step 4: 统计章序号（仅计非 non_content 的 chapter_level 标题）
  ch_map = {heading.start: chapter_num, ...}

Step 5: 对每个 is_node=True 的标题，追踪章/节上下文并提取内容

  上下文追踪规则（for i in range(node_all_idx + 1)）：
    - 跳过 non_content 标题（不污染章名）
    - h['level'] == chapter_level → 更新 current_chapter_name / current_chapter_num
    - h['level'] == section_level AND i < node_all_idx（不含节点自身）→
        若节点(node_h)与 h 不是同级兄弟节点，更新 current_section_name
        （同级判断：node_h.level == section_level AND h.text in node_set）

  内容边界：
    start = node_h['end']（标题行结束）
    end   = min(下一个 is_node 标题的 start,
                下一个 level<=section_level 且非节点的标题 start,
                文档末尾)

Step 6: 构造返回条目
  {
    'parent_chapter_num':  current_chapter_num,
    'parent_chapter_name': current_chapter_name,
    'chapter_name':        current_section_name or current_chapter_name,
    'section_name':        node_h['text'],
    'num':                 global_num,
    'text':                text[start:end].strip(),
  }
  body 为空时跳过（自然过滤掉无内容的父级节点）
```

### 4.3 降级：无 extraction_nodes 时

若 `hierarchy` 中无 `extraction_nodes`（Layer 0 失败或本地降级），调用 `_parse_md_multilevel` 保持旧行为。

---

## 五、`ds_upload` 端点修改

```python
# 旧代码（删除）：
sections = _parse_md_multilevel(cleaned_text, hierarchy)

# 新代码：
if hierarchy.get('extraction_nodes'):
    sections = _parse_md_by_nodes(cleaned_text, hierarchy)
else:
    sections = _parse_md_multilevel(cleaned_text, hierarchy)
```

其余存储逻辑（`INSERT INTO ds_chapters`）完全不变。

---

## 六、不需要变更的内容

| 内容 | 原因 |
|------|------|
| 数据库 Schema | `section_name` 字段已存在，语义完全匹配 |
| `_extract_one` KP 提取函数 | 直接读 `raw_text`，不关心层级 |
| `ds_graph` / `ds_generate` 端点 | 不依赖 leaf_level |
| `index.html` | 不变 |
| `_parse_md_multilevel` | 保留作为降级分支 |
| `_parse_md_to_sections` | 保留（旧兼容） |

---

## 七、实施步骤

| 步骤 | 位置 | 改动 | 状态 |
|------|------|------|------|
| Step 1 | `_clean_headings_with_ai` 函数内 | 替换 prompt（移除 `leaf_level`，增加 `extraction_nodes`，明确粒度原则和 non_content 示例） | ✅ 完成 |
| Step 2 | `_clean_headings_with_ai` 函数内 | 替换返回值校验逻辑 + 三阶段后处理（展开/去重/完整性修复） | ✅ 完成 |
| Step 3 | `rag_routes.py` 新增函数 | 实现 `_parse_md_by_nodes()`，含上下文追踪的三处修正 | ✅ 完成 |
| Step 4 | `ds_upload` 端点 | 改调用：有 extraction_nodes → `_parse_md_by_nodes`，否则 → `_parse_md_multilevel`；移除 fallback_hierarchy 中的 `leaf_level` 字段 | ✅ 完成 |

---

## 八、验证方法

实施后运行以下脚本，预期结果见下方：

```bash
python -X utf8 test_parse_pipeline.py
```

**预期结果**：

```
[章] 第一章农业经营制度
  [节] 第一节农业经营制度演变 > [小节] 一、 农业经营制度的含义
  [节] 第一节农业经营制度演变 > [小节] 二、 农业经营制度变迁
  [节] 第二节农业经营制度的框架 > [小节] 一、 农业经营主体
  [节] 第二节农业经营制度的框架 > [小节] 二、 农业经营形式
  [节] 第二节农业经营制度的框架 > [小节] 三、 农业经营制度的运行机制
  [节] 第三节农业经营制度的发展趋势 > [小节] 一、 新型农业经营体系
  [节] 第三节农业经营制度的发展趋势 > [小节] 二、 农业产业融合
  [节] 第三节农业经营制度的发展趋势 > [小节] 三、 农业社会化服务体系
```

`这一框架的内涵是：` 不出现在任何章节名中。

---

## 九、实际实施记录

### 9.1 实施内容概览

按方案 §七 的 4 个步骤全部完成，实际代码与原设计基本一致，但在实施过程中发现了若干需要额外处理的边界情况，均已在代码中修复。

### 9.2 与原设计的差异点

#### 差异 1：`_clean_headings_with_ai` 增加了三阶段后处理

原设计中校验逻辑较简单，实际实施后发现 DeepSeek 在同一文档的不同调用中可能返回不同粒度的节点（API 非确定性），需要三阶段后处理确保稳定性：

**阶段 1 — 展开**：若 DeepSeek 只返回 section_level 节点（如全部为 H3），自动展开为其 child_level（H4）子节点。展开时跳过 `non_content_texts` 中的标题（OCR 误标标题不作边界），避免跨越 OCR 干扰标题后的子节点被遗漏。

**阶段 2 — 去重**：若 DeepSeek 同时返回 H3 和 H4 节点（父子均在列表），删除 H3 父节点（因 `_parse_md_by_nodes` 中 H3 节点内容为空会被自然过滤，但显式去重更清晰）。

**阶段 3 — 完整性修复**：对被去重删除的 H3 父节点，逐一检查其 child_level 子节点是否都已在列表中，补全 DeepSeek 遗漏的子节点，并按文档中实际出现顺序重排。

这三个阶段解决了测试中发现的关键问题：文档中存在一个 H2 OCR 误标标题（`这一框架的内涵是：`）打断了正常 H3→H4 层级，导致 H4 节点「孤儿化」（不在任何 H3 的直接子节点列表中）。展开阶段跳过非内容标题的边界判断，正确地将该孤儿节点归入正确的 H3 父节点下。

#### 差异 2：`_parse_md_by_nodes` 中上下文追踪的两处修正

**修正 1 — 节点自身不更新 section 上下文**

原设计伪代码：
```
对每个 heading h:
    elif section_level > chapter_level and h['level'] == section_level:
        current_section_name = h['text']
```

若节点本身（is_node=True）处于 section_level，会把自己设为 `current_section_name`，导致 `chapter_name = section_name = 节点自身`（层级关系丢失）。

实际实现：上下文追踪循环范围改为 `range(node_all_idx + 1)`，但对 section_level 的更新增加条件 `i < node_all_idx`，即节点本身不更新 `current_section_name`。

**修正 2 — 同级节点不互为父节点**

在 PPT 类文档中（如 H1=幻灯片标题、H2=子项），相邻 H2 节点是兄弟关系，不应将前一个 H2 节点视为后一个 H2 节点的"节"父级。

实际实现：增加同级兄弟检测条件：
```python
is_sibling_node = (node_h['level'] == section_level and h['text'] in node_set)
if not is_sibling_node:
    current_section_name = h['text']
```

**修正 3 — non_content 标题不污染章名**

原设计在章上下文追踪时未考虑 OCR 误标的非内容 H2 标题（如 `这一框架的内涵是：`）可能被识别为当前章名。

实际实现：追踪循环中增加 `if h['text'] in non_content: continue` 跳过非内容标题。

### 9.3 测试结果

#### 测试用例1：教材型文档（`知识点提取测试用例.md`）

```
[章] 第一章农业经营制度
  [节] 第一节农业经营制度演变 > [小节] 一、 农业经营制度的含义
  [节] 第一节农业经营制度演变 > [小节] 二、 农业经营制度变迁
  [节] 第二节农业经营制度的框架 > [小节] 一、 农业经营主体
  [节] 第二节农业经营制度的框架 > [小节] 二、 农业经营形式
  [节] 第二节农业经营制度的框架 > [小节] 三、 农业经营制度的运行机制
  [节] 第三节农业经营制度的发展趋势 > [小节] 一、 新型农业经营体系
  [节] 第三节农业经营制度的发展趋势 > [小节] 二、 农业产业融合
  [节] 第三节农业经营制度的发展趋势 > [小节] 三、 农业社会化服务体系
```

**结果**：8/8 节点全部命中，`这一框架的内涵是：` 不出现在任何章节名中。✅

#### 测试用例2：PPT 转换文档（`知识点提取测试用例2-无明显章节标识.md`）

- 文档共 49 个标题（H1=幻灯片标题，H2=幻灯片内子项）
- 非内容标题正确过滤：思考与讨论、小结、辞职回乡种地…、项目立项结束 等
- 产出约 30-39 个节点（每次 API 调用略有差异）
- 层级归属基本正确，兄弟节点不互为父节点

**发现的固有限制**（文档格式本身问题）：

| 问题 | 原因 | 影响 |
|------|------|------|
| 存在 6-8 个内容 < 80 字的薄节点 | PPT 封面/过渡页本身无实质内容 | 后续 KP 提取会返回空或低质量结果 |
| chapter_name / section_name 经常一致 | PPT 无真正的章/节/小节三层结构 | 层级展示不佳，但不影响 KP 提取（直接用 `raw_text`） |
| DeepSeek 节点数量轻微波动（±10%） | 标题数量多（49个），API 非确定性 | 偶尔遗漏 1-2 个边缘节点，可重新上传解决 |

---

## 十、PPT 类文档的过渡页过滤方案（已实施）

> 文档状态：**已实施完成**
> 完成日期：2026-03-21

### 10.1 问题描述与分析

PPT 转换文档中，封面页、过渡页等幻灯片产生的节点内容极少甚至为空，是无效 KP 提取消耗。

最初考虑三种方案：
- 方案 A（提取前按长度过滤）：有误伤教材短定义节点的风险（教材 50 字的小节可能是重要定义）
- 方案 B（上传时合并薄节点）：破坏节点语义，风险更高
- **方案 C（Layer 0 源头过滤）**：让 DeepSeek 在 Layer 0 标题分析阶段直接将过渡/封面幻灯片识别为 `non_content_texts`，从源头排除，完全不产生无效节点 ✅ 已选用

### 10.2 实施方案：Layer 0 文档类型感知

#### 修改1：新增 `_detect_doc_type` 函数

在 `_clean_headings_with_ai` 之前新增辅助函数，根据标题层级分布自动判断文档类型：

```python
def _detect_doc_type(headings: list) -> str:
    """PPT型特征：H1>=3次 且 H1占比>=20% 且层级数<=3 且最深层级<=3"""
    h1_count = Counter(h['level'] for h in headings).get(1, 0)
    total = len(headings)
    max_level = max(h['level'] for h in headings)
    distinct_levels = len(set(h['level'] for h in headings))
    is_ppt = (h1_count >= 3 and h1_count/total >= 0.20
              and distinct_levels <= 3 and max_level <= 3)
    return 'ppt' if is_ppt else 'textbook'
```

#### 修改2：差异化 Prompt

根据 `doc_type` 生成不同的 prompt：

**PPT 型 prompt 核心规则**：
- `non_content_texts` 和 `extraction_nodes` 中只放 H1 级别标题（H2 子项不放入任何列表）
- `non_content_texts` 过滤标准：课程封面/过渡套语/以`?`结尾的纯引导性提问页/无关内容/重复H1
- `extraction_nodes` = 有实质知识内容的 H1 幻灯片标题

**教材型 prompt**：与 v3.0 设计完全相同，无变化。

#### 修改3：PPT 文档层级强制覆盖

解析 AI 响应后，对 PPT 文档强制设置：
```python
if doc_type == 'ppt':
    chapter_level = 1
    section_level = 2
```

#### 修改4：`_parse_md_by_nodes` 内容边界修复

PPT H1 节点的内容边界原本因 `h['level'] <= section_level(=2)` 而在第一个 H2 子项处截断，导致内容为空。

修复：将边界条件从 `h['level'] <= section_level` 改为 `h['level'] <= node_h['level']`：
- 对 PPT H1 节点（level=1）：只有下一个非节点 H1 才终止内容，H2 子项包含在内容中 ✅
- 对教材 H4 节点（level=4）：非节点的 H3/H4 仍正确终止内容 ✅（无回归）

### 10.3 测试结果（PPT 文档：知识点提取测试用例2）

**Layer 0 识别**：
- `_detect_doc_type` 正确识别 PPT（H1=19/49=39%，层级数=2）
- `chapter_level=1, section_level=2`（强制覆盖）
- `non_content_texts` 8 个（含：农业项目管理、什么是项目启动?、辞职回乡种地...等）

**切分结果**：
- extraction_nodes：11 个有实质内容的幻灯片
- `_parse_md_by_nodes` 输出：11 个叶子节点，0 个薄节点（均含 199-5866 字实质内容）

**对比 v3.0 旧方案（仅凭文本长度过滤）**：之前同文档产生 6-8 个薄节点；现在直接不产生。

### 10.4 教材型文档无回归

`test_parse_pipeline.py`（教材型文档）结果与 v3.0 完全一致：
- 8/8 节点全部命中
- `这一框架的内涵是：` 不出现在任何章节名字段中
- `_parse_md_by_nodes` 边界修复对教材型文档无任何影响

---

## 十一、测试文件状态

以下测试脚本可长期保留用于回归测试：
- `test_parse_pipeline.py` — 教材型文档完整流程测试（Layer 0 + 切分 + 层级验证）
- `test_ppt_layer0.py` — PPT 型文档测试（文档类型检测 + Layer 0 过滤质量 + 切分预览 + 教材回归）

以下脚本如不再使用可删除：
- `test_layer0_heading.py`
- `test_structure_compare.py`
