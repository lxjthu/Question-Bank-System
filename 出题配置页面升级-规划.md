# 出题配置页面升级规划

> 编写日期：2026-03-21
> 状态：**已完成**（2026-03-21）
> 涉及文件：`app/rag_routes.py`、`app/templates/index.html`

---

## 一、需求描述

### 需求 1：章节筛选加入"节"级别

出题配置页面（AI 智能出题 Tab）的"章节筛选"栏目，目前只显示章（父章名）。需要改成与知识图谱筛选栏一致的两级展示：**章 › 节**，让用户可以精确选择要出题的节范围。

### 需求 2：知识点点击弹出知识图谱详情

出题配置页面右侧的"知识点"列表，目前每项只是一个 checkbox + 文字。需要在知识点名称旁加一个按钮（或将整行变为可点击），点击后：
1. 自动跳转（或弹层/侧滑）显示知识图谱
2. 在图谱中高亮并定位该知识点节点
3. 右侧 KP 详情面板自动打开，显示该知识点的内容、关系等

---

## 二、现状分析（实施前）

### 2.1 出题配置页面数据来源

```
文档勾选变化
    → dsUpdateMeta()
    → GET /api/rag/ds-docs/<doc_id>/kps
    → meta.chapters: [{num, name}]        ← 只有章名（parent_chapter_name）
    → meta.kps:      [{id, chapter_name, chapter_num, name, ...}]
    ↓
_ragAllChapters = [...allChapters.values()]   ← 只有章级别
_ragAllSections = allKps.map(kp => {name, chapter_name, ...})  ← KP 列表
```

`/api/rag/ds-docs/<doc_id>/kps` 端点（`rag_routes.py:2226`）：
- `chapters` 字段：从 `ds_chapters.parent_chapter_name` 去重，**只有章，没有节**
- `kps` 字段：从 `ds_kps` 读取，有 `chapter_name`（父章），但**缺少 `section_name`（节）字段**

### 2.2 知识图谱筛选栏数据来源

```
GET /api/rag/ds-graph
    → data.chapters: [{name, num, section_name}]  ← 有章+节
```

`ds_graph` 端点（`rag_routes.py:2274`）从 `ds_kps` 按 `(chapter_name, section_name)` 分组，有完整的两级章节。

### 2.3 知识点点击现状

出题配置页知识点列表（`renderRagSectionList`，`index.html:5624`）渲染结构：
```html
<label>
  <input type="checkbox" class="rag-section-cb" value="KP名">
  <span>KP名</span>
</label>
```
无任何跳转/弹层逻辑。

知识图谱节点点击逻辑：`_openKpPanel(d)`（`index.html:4818`），接受 D3 节点数据对象（含 `id, name, chapter, section_name, content`）。

---

## 三、实施方案（已落地）

### 3.1 后端：`/api/rag/ds-docs/<doc_id>/kps` 补充 section_name ✅

**文件**：`app/rag_routes.py`，`ds_doc_kps` 函数（line ~2227）

**修改1**：`chapters` 字段改为从 `ds_kps` 按 `(chapter_name, section_name)` 分组，返回带 `section_name` 的两级章节列表：

```python
chapters_raw = conn.execute(
    "SELECT chapter_name, chapter_num, COALESCE(section_name,'') as section_name "
    "FROM ds_kps WHERE doc_id=? ORDER BY chapter_num, id",
    (doc_id,),
).fetchall()
seen_ch: dict = {}
for r in chapters_raw:
    key = (r['chapter_name'], r['section_name'])
    if key not in seen_ch:
        seen_ch[key] = r['chapter_num']
chapters_list = sorted(
    [{'name': ch, 'section_name': sec, 'num': num} for (ch, sec), num in seen_ch.items()],
    key=lambda x: (x['num'], x['section_name'])
)
```

**修改2**：`kps` 字段新增 `section_name` 列：

```python
kps = conn.execute(
    "SELECT id, chapter_name, chapter_num, COALESCE(section_name,'') as section_name, "
    "kp_name, kp_content, relations_json, "
    "teaching_focus, knowledge_type, cognitive_dimension "
    "FROM ds_kps WHERE doc_id=? ORDER BY chapter_num, id",
    (doc_id,),
).fetchall()
# 返回时包含 section_name 字段
```

### 3.2 前端：章节筛选栏改为两级展示 ✅

**文件**：`app/templates/index.html`

**修改点1**：`dsUpdateMeta` 函数 — `allChapters` 去重改用 `名|||节` 联合键，`_ragAllSections` 新增 `section_name` 字段：

```javascript
const key = ch.name + '|||' + (ch.section_name || '');
allChapters.set(key, ch);

_ragAllSections = allKps.map(kp => ({
    num: kp.id,
    name: kp.name,
    chapter_name: kp.chapter_name,
    section_name: kp.section_name || '',   // 新增
    chapter_num: kp.chapter_num,
    display_name: kp.name,
}));
```

**修改点2**：`renderRagChapterList` — checkbox value 改为 `章名|||节名`，显示"章 › 节"两级：

```javascript
const val = c.name + '|||' + secName;
const display = secName
    ? `${escapeHtml(c.name)} <span style="color:#aaa">›</span> <span style="color:#555">${escapeHtml(secName)}</span>`
    : escapeHtml(c.name);
```

**修改点3**：`filterRagSections` — 联动过滤改用联合键比对：

```javascript
filtered = filtered.filter(s => {
    const key = s.chapter_name + '|||' + (s.section_name || '');
    return checkedChapters.includes(key);
});
```

### 3.3 前端：知识点点击弹出知识图谱 ✅

**`renderRagSectionList`**：每个 KP 条目末尾加紫红色图谱图标按钮，调用 `_jumpToKpInGraph(s.num, s.name)`。

**新增函数**（实际落地版，含 Bug 修正）：

```javascript
async function _jumpToKpInGraph(kpId, kpName) {
    const nodeId = 'kp_' + kpId;  // 图谱节点 id 格式为 "kp_<整数>"

    // 1. 若图谱模态未打开或数据未加载，先调用 showDsKgModal()
    if (_dsKgAllNodes.length === 0) {
        showDsKgModal();
    }

    // 2. 等待图谱数据加载完成
    try {
        await _waitForKgLoaded();
    } catch (e) {
        alert('知识图谱加载超时，请先点击"知识图谱"按钮确认图谱已加载后再试。');
        return;
    }

    // 3. 查找节点（按 id 精确匹配，按名称兜底）
    const node = _dsKgAllNodes.find(n => n.id === nodeId || n.name === kpName);
    if (!node) { alert(...); return; }

    // 4. 确保节点在筛选下可见
    const nodeKey = (node.chapter || '') + '|||' + (node.section_name || '');
    const cb = document.querySelector(`.ds-kg-ch-cb[value="${CSS.escape(nodeKey)}"]`);
    if (cb && !cb.checked) {
        cb.checked = true;
        if (typeof _applyDsKgFilter === 'function') _applyDsKgFilter();
        await new Promise(r => setTimeout(r, 300));
    }

    // 5. 打开详情面板 + 高亮节点
    if (typeof _openKpPanel === 'function') _openKpPanel(node);
    _highlightKgNode(nodeId);
}
```

### 3.4 后端：`ds_generate` 端点支持 `章名|||节名` 格式筛选 ✅

```python
if chapters:
    chapter_filters = []
    for ch_val in chapters:
        if '|||' in ch_val:
            ch_name, sec_name = ch_val.split('|||', 1)
            chapter_filters.append((ch_name.strip(), sec_name.strip()))
        else:
            chapter_filters.append((ch_val.strip(), None))
    parts = []
    for ch_name, sec_name in chapter_filters:
        if sec_name:
            parts.append("(chapter_name=? AND COALESCE(section_name,'')=?)")
            params.extend([ch_name, sec_name])
        else:
            parts.append("chapter_name=?")
            params.append(ch_name)
    query += " AND (" + " OR ".join(parts) + ")"
```

---

## 四、涉及文件和函数汇总

### 后端（`app/rag_routes.py`）

| 函数 | 修改内容 |
|------|---------|
| `ds_doc_kps`（line ~2227） | `chapters` 改为从 `ds_kps` 按 `(chapter_name, section_name)` 分组返回；`kps` 新增 `section_name` 字段 |
| `ds_generate` | 解析 `章名|||节名` 格式，按 `chapter_name AND COALESCE(section_name,'')` 联合过滤；向后兼容纯章名格式 |

### 前端（`app/templates/index.html`）

| 函数/位置 | 修改内容 |
|-----------|---------|
| `dsUpdateMeta` | `allChapters` 去重改用 `名|||节` 联合键；`_ragAllSections` 新增 `section_name` 字段 |
| `filterRagSections` | 联动过滤改用 `章名|||节名` 比对 |
| `renderRagChapterList` | checkbox value 改为 `章名|||节名`，显示"章 › 节"两级 |
| `renderRagSectionList` | KP 条目末尾加图谱跳转按钮 |
| 新增 `_jumpToKpInGraph(kpId, kpName)` | 打开图谱模态 → 等待加载 → 定位节点 → 展开面板 → 高亮 |
| 新增 `_highlightKgNode(nodeId)` | D3 圆圈放大高亮，3 秒后恢复 |
| 新增 `_waitForKgLoaded()` | 轮询 `_dsKgAllNodes`，最多等待 8 秒 |

---

## 五、关键 Bug 修正记录

### Bug 1：知识图谱加载超时

**现象**：点击知识点旁的图谱按钮，弹出"知识图谱加载超时"错误；但直接点击"知识图谱"按钮可以正常加载。

**根因**：规划阶段误以为知识图谱是 Tab 页面，实际上是**模态窗口**（`#ds-kg-modal`）。原代码用 `document.querySelector('[data-tab="ds-kg"]')` 选择器查找不存在的元素，导致模态从未打开，`_dsKgAllNodes` 永远为空，轮询超时。

**修复**：将 `kgTab.click()` 替换为直接调用 `showDsKgModal()`（当 `_dsKgAllNodes` 为空时），这才是打开模态并触发数据加载的正确入口。

### Bug 2：节点 ID 格式不匹配

**现象**：即使图谱加载成功，也找不到对应节点，`_highlightKgNode` 也不生效。

**根因**：`ds_graph` 端点生成的节点 id 格式为字符串 `"kp_<数据库id>"`（如 `"kp_42"`），而按钮传入的 `s.num` 是整数（如 `42`），`n.id === kpId` 类型不匹配，永远为 false。

**修复**：在函数入口统一转换：`const nodeId = 'kp_' + kpId`，后续所有比对和高亮均使用 `nodeId`。

---

## 六、实施顺序（已完成）

1. ✅ **Step 1**：后端 `ds_doc_kps` 端点补充 `section_name`
2. ✅ **Step 2**：前端章节筛选栏改为两级（渲染 + 联动逻辑）
3. ✅ **Step 3**：后端 `ds_generate` 支持联合键过滤
4. ✅ **Step 4**：前端 KP 点击跳转知识图谱（`_jumpToKpInGraph` + 高亮）
5. ✅ **Step 5（补丁）**：修复加载超时 Bug（选择器错误 + 节点 ID 格式不匹配）

---

## 七、不需要变更的内容

| 内容 | 原因 |
|------|------|
| `ds_graph` 端点 | 已正确返回两级章节，无需修改 |
| `_buildDsKgFilterPanel` | 知识图谱筛选栏已支持两级，无需修改 |
| `_openKpPanel` / `_renderKpPanel` | 详情面板逻辑完整，直接复用 |
| 数据库 Schema | `ds_kps.section_name` 字段已存在 |
| KP 详情面板 HTML 结构 | 复用现有 `ds-kp-panel` |
