# 打包发布说明

## 概览

| 平台 | 产物 | 必须在哪台电脑上操作 |
|------|------|-------------------|
| Windows | `exam-system_vX.X.X_win64.zip` | Windows 电脑 |
| macOS Intel | `试题管理系统_vX.X.X_x86_64.dmg` | **必须在 Intel Mac 上** |
| macOS Apple Silicon | `试题管理系统_vX.X.X_arm64.dmg` | **必须在 M 系列 Mac 上** |
| macOS 通用版（可选） | `试题管理系统_vX.X.X_universal2.dmg` | **必须在 M 系列 Mac 上** |

> PyInstaller 打包是平台限定的：Windows 只能打 Windows 包，Mac 才能打 Mac 包。

---

## macOS 芯片类型说明

### Intel 芯片 vs Apple Silicon（M 系列）的区别

| 对比项 | Intel Mac | Apple Silicon（M1/M2/M3/M4） |
|--------|-----------|------------------------------|
| CPU 架构 | x86_64 | arm64 |
| 上市年份 | 2006–2020 | 2020 至今 |
| 识别方法 | `uname -m` 输出 `x86_64` | `uname -m` 输出 `arm64` |
| Python 安装包 | macOS 64-bit installer | macOS arm64 installer |
| 打包产物架构 | x86_64 | arm64 |
| 能否运行对方的包 | ❌ 无法运行 arm64 包 | ✅ 可通过 Rosetta 2 运行 x86_64 包（有性能损耗） |

> **Rosetta 2**：苹果提供的兼容层，让 M 系列 Mac 能运行 Intel 的 x86_64 应用，但性能约降低 10–30%，且部分底层 API 行为有差异，不推荐长期依赖。

### 三种打包方案对比

| 方案 | 文件名后缀 | 文件大小 | Intel Mac | Apple Silicon Mac | 推荐场景 |
|------|-----------|---------|-----------|-------------------|---------|
| **arm64 专版** | `_arm64.dmg` | 小（约 60–80 MB） | ❌ 无法运行 | ✅ 原生性能 | 目前主流（M 系列占比越来越高） |
| **x86_64 专版** | `_x86_64.dmg` | 小（约 60–80 MB） | ✅ 原生性能 | ✅ Rosetta 模拟（略慢） | 需要兼容老 Mac |
| **Universal2 通用版** | `_universal2.dmg` | 大（约 120–160 MB，两份代码合并） | ✅ 原生性能 | ✅ 原生性能 | 追求最大兼容性 |

**当前项目默认只发布 arm64 版**，因为 M 系列 Mac 已占新 Mac 销量绝大多数，且 x86_64 用户可通过 Rosetta 运行 arm64 包（苹果已将此作为官方降级路径）。

---

## Windows 打包（EXE）

在项目根目录**双击 `build_win.bat`**，等待完成即可。

脚本会自动：创建隔离 Python 环境 → 安装依赖 → PyInstaller 打包 → 压缩为 zip

产物：`dist/exam-system_vX.X.X_win64.zip`

---

## macOS 打包（DMG）

> 以下步骤默认打 **arm64 版**（适用 M 系列 Mac）。
> 如需打其他架构版本，请参阅最后的「[其他架构打包方案](#其他架构打包方案)」章节。

### 第一步：把代码弄到 Mac 上

打开 Mac 上的"终端"（Terminal，在启动台 → 其他里找），输入：

```bash
git clone https://github.com/lxjthu/Question-Bank-System.git
cd Question-Bank-System
git checkout ds-text-only
```

也可以用 U 盘 / AirDrop 把项目文件夹复制过来，然后在终端里 `cd` 进入项目目录。

---

### 第二步：安装 Python

1. 打开浏览器，访问 **https://www.python.org/downloads/macos/**
2. 下载最新的 **macOS 64-bit universal2 installer**（页面上最显眼的按钮）
3. 双击下载的 `.pkg` 文件，一路点"继续"安装

安装完后，在终端验证：

```bash
python3 --version
```

看到 `Python 3.x.x` 即成功。

> ⚠️ 如果你的 Mac 是 M1/M2/M3 芯片（Apple Silicon），必须下载 **arm64 原生版**，不要用 Rosetta 模拟的 Python，否则打出来的包运行会很慢。
>
> 验证方法：`python3 -c "import platform; print(platform.machine())"` 输出 `arm64` 才对。

---

### 第三步：运行构建脚本

```bash
# 给脚本加执行权限（只需做一次）
chmod +x build_mac.sh

# 开始构建
./build_mac.sh
```

脚本会自动完成所有事情：创建隔离 Python 环境 → 安装依赖 → 打包 `.app` → 制作 `.dmg`

耗时约 **3～5 分钟**，最后看到以下内容即成功：

```
🎉 构建成功！
   DMG 路径：.../dist/试题管理系统_vX.X.X_arm64.dmg
```

---

### 第四步：本地测试

1. 打开 `dist/` 文件夹，双击 `.dmg`
2. 将「试题管理系统」图标拖入「Applications」文件夹
3. 在启动台找到并双击运行
4. 若弹出"无法打开，因为来自身份不明的开发者"：
   前往 **系统设置 → 隐私与安全性 → 仍要打开**

启动后约 2 秒，浏览器会自动打开，即可使用。

---

### 第五步：上传到 GitHub Release

测试通过后，在终端执行（版本号替换为实际版本）：

```bash
gh release upload v1.6.2 "dist/试题管理系统_v1.6.2_arm64.dmg" \
  --repo lxjthu/Question-Bank-System
```

如果没有 `gh` 命令，先安装：

```bash
brew install gh
gh auth login
```

如果连 Homebrew 也没有，先安装 Homebrew：

```bash
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
```

---

## 常见报错

| 报错信息 | 原因 | 解决方法 |
|---------|------|---------|
| `command not found: python3` | Python 未安装 | 重新执行第二步 |
| `command not found: ./build_mac.sh` | 没有执行权限 | 先运行 `chmod +x build_mac.sh` |
| `Python 架构为 x86_64` | 用了 Rosetta Python | 重装 arm64 版 Python |
| 打开 app 提示"来自身份不明的开发者" | 未代码签名（正常现象） | 系统设置 → 隐私安全 → 仍要打开 |
| 浏览器没有自动打开 | 偶发 | 手动输入 `http://127.0.0.1:5050` |

---

## 发布新版本完整流程

1. 修改 `VERSION` 文件中的版本号（如改为 `1.7.0`）
2. 提交并打 tag：
   ```bash
   git add VERSION
   git commit -m "chore: bump version to 1.7.0"
   git tag v1.7.0
   git push && git push --tags
   ```
3. 在 GitHub 创建 Release：
   ```bash
   gh release create v1.7.0 --title "v1.7.0 试题管理系统" \
     --repo lxjthu/Question-Bank-System
   ```
4. 在 **Windows** 上运行 `build_win.bat`，上传 zip
5. 在 **Mac** 上运行 `./build_mac.sh`，上传 dmg

---

## 其他架构打包方案

### 方案 A：在 Intel Mac 上打 x86_64 版

适用场景：你有一台老 Intel Mac，需要发布 x86_64 专版。

**第一步**：确认当前机器是 Intel 架构：

```bash
uname -m   # 应输出 x86_64
```

**第二步**：安装 x86_64 版 Python（从 python.org 下载普通 macOS installer，非 universal2 版）：

```bash
# 验证 Python 架构
python3 -c "import platform; print(platform.machine())"
# 应输出 x86_64
```

**第三步**：修改构建脚本中的 DMG 文件名，将 `arm64` 改为 `x86_64`，然后正常运行：

```bash
# 临时覆盖文件名（不修改脚本的话）
DMG_SUFFIX=x86_64 ./build_mac.sh
```

或直接编辑 `build_mac.sh` 第 22 行：

```bash
# 改为：
DMG_NAME="${APP_NAME}_v${VERSION}_x86_64.dmg"
```

产物：`dist/试题管理系统_vX.X.X_x86_64.dmg`

---

### 方案 B：在 M 系列 Mac 上打 Universal2 通用版

Universal2 是苹果官方的"胖二进制"格式，一个包同时包含 x86_64 和 arm64 两套代码，所有 Mac 均可原生运行，无需 Rosetta。

**代价**：文件体积约是单架构版的 2 倍（120–160 MB vs 60–80 MB）。

**前提条件**：必须在 M 系列 Mac 上操作，且安装的是 **universal2 版 Python**。

**第一步**：安装 universal2 Python：

```bash
# 从 python.org 下载 "macOS universal2 installer"（文件名含 universal2）
# 安装后验证：
python3 -c "import platform; print(platform.machine())"
# M 系列上输出 arm64，说明当前进程是 arm64，但 universal2 Python 能编译两种架构
```

**第二步**：安装支持 universal2 的 PyInstaller（需 5.x 及以上）：

```bash
pip install "pyinstaller>=5.0"
```

**第三步**：修改 `exam_system.spec`，在 `exe = EXE(...)` 中添加 `target_arch='universal2'`，或在 Analysis 块中指定：

```python
# exam_system.spec 中（在 a = Analysis(...) 后）
exe = EXE(
    a.scripts,
    ...
    target_arch='universal2',   # ← 添加这一行
)
```

或通过命令行 flag（无需改 spec 文件）：

```bash
pyinstaller exam_system.spec --target-arch universal2
```

**第四步**：修改 `build_mac.sh` 中的 DMG 文件名为 `universal2`，然后运行：

```bash
./build_mac.sh
```

产物：`dist/试题管理系统_vX.X.X_universal2.dmg`

**验证产物是否真的是 universal2**：

```bash
file dist/试题管理系统.app/Contents/MacOS/试题管理系统
# 输出应包含：Mach-O universal binary with 2 architectures: [x86_64:Mach-O 64-bit executable x86_64] [arm64:Mach-O 64-bit executable arm64]
```

---

### 方案 C：在 M 系列 Mac 上用 Rosetta 打 x86_64 版（不推荐）

理论上可行，但结果不可预测，不推荐：

```bash
# 用 Rosetta 启动终端（在终端图标上右键 → 显示简介 → 勾选"使用 Rosetta 打开"）
# 此时 uname -m 会输出 x86_64
arch -x86_64 python3 -m pip install pyinstaller
arch -x86_64 pyinstaller exam_system.spec
```

问题：部分 Python 依赖库（尤其是含 C 扩展的，如 lxml）在混合环境下容易出现架构冲突错误。

---

### 哪种方案适合我？

```
用户主要是新 Mac（M 系列）？
├── 是 → 只发布 arm64 版即可（当前方案）
└── 否，还有很多老 Intel Mac 用户
    ├── 预算/精力有限 → 发布 x86_64 版（Intel 原生，M 系列用 Rosetta 兼容）
    └── 追求完美兼容 → 发布 universal2 版（两种 Mac 均原生运行，但包更大）
```

**目前项目默认方案**：仅发布 arm64 版。Intel Mac 用户可通过系统提示安装 Rosetta 2 后运行，苹果官方支持此降级路径，无需用户手动操作。
