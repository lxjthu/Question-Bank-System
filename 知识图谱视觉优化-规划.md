# 知识图谱视觉优化规划

> 编写日期：2026-03-21
> 状态：**已完成**（2026-03-21）
> 涉及文件：`app/templates/index.html`

---

## 一、需求描述

1. **节点文字改为黑色**：当前节点内的文字是白色（`fill: '#fff'`），可读性依赖背景颜色深浅，有些颜色下文字不够清晰。改为黑色/深色。
2. **选中节点高亮**：在知识图谱中点击某个节点（打开详情面板）后，该节点应有持续的视觉高亮标记（如略微变大、白色描边），与其他节点有明显区分。关闭面板后高亮消失。

---

## 二、现状分析

### 2.1 文字颜色（`_renderDsKg`，line ~4764）

```javascript
nodeG.append('text')
    .attr('font-size', 9).attr('fill', '#fff').attr('font-weight', '600')
    ...
```

- 当前文字：白色 `#fff`，固定不变
- 节点背景：按章节着色（`colorOf(d.chapter)`），颜色深浅不一
- 问题：浅色节点上白色文字对比度低，难以辨认

### 2.2 选中节点高亮

- 当前点击事件（line ~4782）：只调用 `_openKpPanel(d)`，没有任何视觉反馈
- `_highlightKgNode(nodeId)`（由 `_jumpToKpInGraph` 调用）：有高亮效果，但 3 秒后自动复原，且不能持续标记"当前选中"
- `_closeKpPanel`（line ~4831）：只收起面板，不做任何节点样式重置

---

## 三、实施方案

### 3.1 节点文字改为黑色

**方案**：将 `fill` 改为深色（`#1a1a1a`），同时加白色描边（`stroke`）形成"晕染"效果，确保任何颜色背景下文字均清晰可读。利用 SVG 的 `paint-order: stroke` 让描边渲染在填充之下（描边不覆盖文字本身）。

**修改位置**：`_renderDsKg` 函数，line ~4764

```javascript
// 旧
nodeG.append('text')
    .attr('text-anchor', 'middle').attr('dy', '0.35em')
    .attr('font-size', 9).attr('fill', '#fff').attr('font-weight', '600')
    .attr('pointer-events', 'none')
    .text(d => d.name.length > 7 ? d.name.slice(0, 7) + '…' : d.name);

// 新
nodeG.append('text')
    .attr('text-anchor', 'middle').attr('dy', '0.35em')
    .attr('font-size', 9).attr('fill', '#1a1a1a').attr('font-weight', '700')
    .attr('stroke', 'rgba(255,255,255,0.75)').attr('stroke-width', 2.5)
    .style('paint-order', 'stroke')
    .attr('pointer-events', 'none')
    .text(d => d.name.length > 7 ? d.name.slice(0, 7) + '…' : d.name);
```

> `paint-order: stroke` 让描边先画、填充后画，从而白色描边作为"光晕"出现在文字边缘之外，黑色字体叠加在上层，视觉效果清晰。

### 3.2 选中节点持续高亮

**方案**：

1. 新增全局变量 `_dsKgSelectedNodeId`，记录当前已打开详情面板的节点 id
2. `_renderDsKg` 初始渲染时，按 `_dsKgSelectedNodeId` 决定节点初始样式（支持筛选重渲染后保持高亮）
3. 节点点击时：设置 `_dsKgSelectedNodeId = d.id`，立即更新所有圆的样式（无需重渲染）
4. `_closeKpPanel` 关闭面板时：清除 `_dsKgSelectedNodeId = null`，恢复所有圆的默认样式
5. `_jumpToKpInGraph` 跳转时：通过设置 `_dsKgSelectedNodeId` 并更新样式，替代原来临时性的 `_highlightKgNode`（原函数可保留供独立调用）

**高亮样式规格**：

| 属性 | 默认值 | 选中值 |
|------|--------|--------|
| `r`（半径） | 22 | 30 |
| `stroke`（描边颜色） | `colorOf(d.chapter)`（章节色） | `#ffe000`（亮黄色） |
| `stroke-width` | 2.5 | 5 |
| `stroke-opacity` | 0.4 | 1 |
| `fill-opacity` | 0.82 | 1（完全不透明） |

**修改点1**：全局变量声明（在 `_dsKgAllNodes` 等声明附近）

```javascript
let _dsKgSelectedNodeId = null;
```

**修改点2**：`_renderDsKg` 节点圆属性（支持初始状态反映选中）

```javascript
nodeG.append('circle')
    .attr('r', d => d.id === _dsKgSelectedNodeId ? 26 : 22)
    .attr('fill', d => colorOf(d.chapter))
    .attr('fill-opacity', d => d.id === _dsKgSelectedNodeId ? 1 : 0.82)
    .attr('stroke', d => d.id === _dsKgSelectedNodeId ? 'rgba(255,255,255,0.95)' : colorOf(d.chapter))
    .attr('stroke-width', d => d.id === _dsKgSelectedNodeId ? 4 : 2.5)
    .attr('stroke-opacity', d => d.id === _dsKgSelectedNodeId ? 1 : 0.4);
```

**修改点3**：节点点击事件，设置选中状态并更新样式

```javascript
.on('click', (e, d) => {
    e.stopPropagation();
    if (tt) tt.style.display = 'none';
    _dsKgSelectedNodeId = d.id;
    // 立即更新所有节点样式（不触发重渲染）
    d3.selectAll('#ds-kg-container circle')
        .attr('r', n => n.id === _dsKgSelectedNodeId ? 26 : 22)
        .attr('fill-opacity', n => n.id === _dsKgSelectedNodeId ? 1 : 0.82)
        .attr('stroke', n => n.id === _dsKgSelectedNodeId ? 'rgba(255,255,255,0.95)' : _dsKgColorOf(n.chapter))
        .attr('stroke-width', n => n.id === _dsKgSelectedNodeId ? 4 : 2.5)
        .attr('stroke-opacity', n => n.id === _dsKgSelectedNodeId ? 1 : 0.4);
    _openKpPanel(d);
});
```

> 注意：这里用 `_dsKgColorOf` 而非局部的 `colorOf`，因为 `colorOf` 是 `_renderDsKg` 内的局部变量，`d3.selectAll` 的回调在作用域外也可能被调用。`_dsKgColorOf` 是全局可访问的。

**修改点4**：`_closeKpPanel` 关闭时清除选中状态

```javascript
function _closeKpPanel() {
    if (_kpPanelEditing) {
        if (!confirm('有未保存的修改，关闭将丢弃，是否继续？')) return;
    }
    _kpPanelEditing = false;
    _kpPanelData = null;
    document.getElementById('ds-kp-panel').style.width = '0';
    // 清除节点高亮
    _dsKgSelectedNodeId = null;
    d3.selectAll('#ds-kg-container circle')
        .attr('r', 22).attr('fill-opacity', 0.82)
        .attr('stroke', d => _dsKgColorOf(d.chapter))
        .attr('stroke-width', 2.5).attr('stroke-opacity', 0.4);
}
```

**修改点5**：`_jumpToKpInGraph` 中设置选中状态（替代临时高亮）

```javascript
// 5. 打开详情面板并持续高亮（替代原来的 _highlightKgNode 临时高亮）
_dsKgSelectedNodeId = nodeId;
d3.selectAll('#ds-kg-container circle')
    .attr('r', n => n.id === _dsKgSelectedNodeId ? 26 : 22)
    .attr('fill-opacity', n => n.id === _dsKgSelectedNodeId ? 1 : 0.82)
    .attr('stroke', n => n.id === _dsKgSelectedNodeId ? 'rgba(255,255,255,0.95)' : _dsKgColorOf(n.chapter))
    .attr('stroke-width', n => n.id === _dsKgSelectedNodeId ? 4 : 2.5)
    .attr('stroke-opacity', n => n.id === _dsKgSelectedNodeId ? 1 : 0.4);
if (typeof _openKpPanel === 'function') _openKpPanel(node);
```

---

## 四、涉及函数汇总

| 函数/位置 | 修改内容 |
|-----------|---------|
| 全局变量区（line ~4343） | 新增 `let _dsKgSelectedNodeId = null` |
| `_renderDsKg`，circle 属性（line ~4757） | 按 `_dsKgSelectedNodeId` 动态决定 r、stroke、fill-opacity |
| `_renderDsKg`，text 属性（line ~4764） | fill 改 `#1a1a1a`，加白色 stroke + `paint-order:stroke` |
| `_renderDsKg`，click 事件（line ~4782） | 设置 `_dsKgSelectedNodeId`，`d3.selectAll` 更新所有圆样式 |
| `_closeKpPanel`（line ~4831） | 关闭时清除 `_dsKgSelectedNodeId`，恢复默认样式 |
| `_jumpToKpInGraph`（新增函数） | 设置 `_dsKgSelectedNodeId` 并更新样式，替代 `_highlightKgNode` 临时逻辑 |

---

## 五、不需要变更的内容

| 内容 | 原因 |
|------|------|
| `_highlightKgNode` 函数 | 保留，供独立调用场景（如临时闪烁效果），但 `_jumpToKpInGraph` 不再调用它 |
| `_openKpPanel` / `_renderKpPanel` | 无需修改，选中状态由调用方管理 |
| 节点颜色映射 `_dsKgColorOf` | 无需修改，仍按章节着色 |
| 边（link）样式 | 无需修改 |
| 图例 | 无需修改 |

---

## 六、实施顺序（已完成）

1. ✅ **Step 1**：添加全局变量 `_dsKgSelectedNodeId`
2. ✅ **Step 2**：修改 `_renderDsKg` 文字颜色（黑色 + 白色描边）
3. ✅ **Step 3**：修改 `_renderDsKg` 节点圆属性支持选中样式
4. ✅ **Step 4**：修改 `_renderDsKg` 点击事件，设置选中并更新样式
5. ✅ **Step 5**：修改 `_closeKpPanel` 清除选中
6. ✅ **Step 6**：修改 `_jumpToKpInGraph` 改为持久高亮
